# Code Run — 2026-06-20 — SP6 Testing & CI + Cross-SP Fixes
Branch: fixing-cors   Base: main

## Tasks completed

| Sub-project | Task | Status | Commit | Files | Tests |
|---|---|---|---|---|---|
| 06-testing-and-ci | Tasks 1–7 — Backend pytest infra + restructured test suite | done | fd7f0a5 | 44 files | — |
| 06-testing-and-ci | Task 8 — Frontend RTL setup + tests | done (pre-existing) | 351ac48 | 9 files | 63 passed |
| infra-fix | pytest importmode; delete stale flat tests + juno_metrics.py | done | 08f5189 | pyproject.toml, 35 deletions | 186 passed |
| 02-route-consolidation | Fix stale GCS prefix, RESULT_SENTINEL, batch scoring, docstrings | done | 1e61e48 | routes/care_plan.py, routes/batch.py, utils/juno_logger.py | — |
| 06-testing-and-ci | Tasks 9+10 — CI workflow, requirements-dev.txt, coverage | done | e955220 | ci.yml, requirements-dev.txt, pyproject.toml, vite.config.ts | 186+63 passed |
| 06-testing-and-ci | Task 11 — Convert all 16 tests to pytest; fix import style | done | 5d804a8 | 16 test files | 199 passed |
| 06-testing-and-ci | Task 5 — New util tests (scoring, term_detection, text_norm, jargon_db) | done | d9b0638 | 4 new test files | 295 passed |
| 06-testing-and-ci | Task 7 — Rewrite route/pipeline/observability tests for current API | done | 1d13d90 | 5 test files | 295 passed |

**Final counts: backend 295 passed / frontend 63 passed — 0 failures.**

## Deviations from the PRD/TASKS

- **tests/*/`__init__.py` deleted:** The task originally said to create `__init__.py` in each test subdir if "package-style collection" works. It does NOT work — the empty `__init__.py` files shadowed the real `backend/routes`, `backend/models`, `backend/utils` packages, causing `ImportError` at conftest load time. All 6 `__init__.py` stubs were deleted. `importmode = "importlib"` was added to `pyproject.toml` as the proper fix. This is a net improvement.

- **test_grading_route.py doc shape:** Firestore doc shape updated to match current `output_data.care_plan.raw.{text, clarified_text}` structure (reflects SP1/SP2 actual output, not old shape in TASKS.md snippet).

- **test_pipeline_happy_path.py patches `PIPELINES` dict, not bare function:** The route does `PIPELINES[version]()` not `run_care_plan_pipeline()` directly, so the correct monkeypatch target is `routes.care_plan.PIPELINES`.

- **test_app_observability.py: all-skip stubs replaced with real tests:** The worktree agent wrote skip stubs; they were replaced with real tests covering `register_sink`/`resolve_sink`, `InMemorySink` event capture, `CodeMarker.execute` success/failure, and app-level `JunoSink` registration.

## Review findings fixed (from cross-SP reviews)

| SP | Finding | Fixed | Commit |
|---|---|---|---|
| All | `tests/*/.__init__.py` shadow real packages → ImportError | ✅ | 08f5189 |
| SP2 | `_UPLOAD_PREFIX = "simplify-uploads"` stale GCS prefix | ✅ | 1e61e48 |
| SP2 | `batch.py` hardcoded `"__result__"` instead of `RESULT_SENTINEL` | ✅ | 1e61e48 |
| SP2 | Batch route discarded `before_score`/`after_score` from sentinel | ✅ | 1e61e48 |
| SP2 | `juno_logger.py` docstrings referenced old `/simplify/v1-2` | ✅ | 1e61e48 |
| SP3 | `utils/juno_metrics.py` still on disk (should have been deleted in SP3) | ✅ | 08f5189 |
| SP6 | Stale flat test files physically present at `tests/` root (untracked) | ✅ | 08f5189 |

## Follow-ups / new issues discovered

- **SP4 minor (non-blocking):** `routes/care_plan.py:521` and `routes/batch.py:185` use `getattr(g, "session_id", "")` instead of `g.session_id` directly. This deviates from the SP4 Task 6 spec but is non-breaking (empty-string fallback, no security issue). Recommend fixing in a follow-up PR for consistency.

- **SP5 minor (non-blocking):** `OutputGradingCard.tsx` reads `X-Session-Id` but not `X-Trace-Id` on the grade endpoint — minor inconsistency, PRD only required `X-Session-Id` there.

- **Test coverage gaps deferred:** `normalizeOutput.test.ts` missing the `care_plan` envelope key path test; `CarePlanPage.tsx` has no RTL tests; `VersionsPage.tsx` has no test; `api/datasets.ts` has no test file. These are acceptable given the SP6 scope — could be added in a later PR.

- **`outputVersion.test.ts` / `router.test.ts`:** SP6 TASKS.md mentioned these but the source files were deleted by SP5; these tests have no source to test and remain absent (correct).

## Still requires you (from PRD §8 + this run)

1. **Branch protection on `main`** — Already done by owner. After `ci.yml`'s first PR run, select `backend` + `frontend` check names in GitHub → Settings → Branches as required checks.
2. **`VITE_DEFAULT_VERSION` env var** — Remove from `.env*` files if present (now a hardcoded constant).
3. **Cloud Console setup** (SP4): log-based metrics, Metrics Explorer dashboard, Trace Explorer `session.id` filter, set `SERVICE_VERSION` on Cloud Run, verify `roles/cloudtrace.agent`.
4. **Coverage threshold ratcheting** — Once a baseline exists, add `--cov-fail-under=N` to enforce a minimum. Currently reporting-only per PRD §9.3.
