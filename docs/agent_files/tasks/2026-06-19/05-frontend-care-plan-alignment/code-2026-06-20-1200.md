# Code Run — 2026-06-20 — 05-frontend-care-plan-alignment (all tasks)
Branch: fixing-cors   Base: main

## Tasks completed
| Task | Status | Commit | Files | Tests |
|---|---|---|---|---|
| Tasks 1+2 — Delete dead version pages + routing helpers | done | 10a819b | 5 deleted, 1 edited | build clean |
| Task 3 — Move CSS to care-plan/, rename SimplifyPage→CarePlanPage | done | ff68b74 | 2 moved, 2 edited | build clean |
| Task 4 — Remove dynamic version chooser, keep static VersionsPage | done | 4227863 | 1 deleted, 5 edited | build clean |
| Task 5 — Swap /simplify* HTTP paths to /care_plan* | done | 21bc255 | 3 edited | build clean |
| Tasks 6+7 — Rename SimplifyOutput→CarePlanInternal, AppointmentNote→CarePlanContent, care_plan inner key | done | d0ed285 | 9 edited | tsc clean, build clean |
| Tasks 8+9 — Correlation headers + UI copy alignment | done | 329a6a6 | 7 edited | build clean |
| Task 10 — Final sweep | done | a0c5cd1 | 5 edited/added | all checks pass |

## Deviations from the PRD/TASKS
- Tasks 8+9 also updated `SplitView.tsx`, `LoginPage.tsx`, `buildPdfHtml.ts`, and `config.ts` description text to remove "Simplify" framing — within the spirit of §8.5 (reword approved) even though those files weren't listed explicitly.
- Task 4 agent also updated `CarePlanPage.tsx` to use `CARE_PLAN_API_PATH` (moving some Task 5 work forward) — no impact on correctness.
- Task 10 also added `eslint.config.js` (ESLint v9 flat config file that was never committed to the repo), fixed a synchronous `setState` inside a `useEffect` in `SplitView.tsx`, replaced `any` types with `unknown`/typed casts in `grading.ts` and `normalizeOutput.ts`, and updated a JSDoc example in `logger.ts` (`SimplifyPage` → `CarePlanPage`).

## Follow-ups / new issues discovered
- SP6 should add/adjust tests per PRD §7: `normalizeOutput.test.ts`, `CarePlanView.tsx` (RTL), `OutputGradingCard.tsx` (RTL), `CarePlanPage.tsx` (RTL/integration with X-Session-Id header mock), `ConfigurationCard.tsx`, `VersionsPage.tsx`.
- `SimplifiedCarePlan` type name in `envelope.ts` was intentionally kept (it is a TS-internal type, not a wire key or user-facing string per PRD §4.4 — `SimplifiedCarePlan` is already care-plan-named).

## Still requires you (from PRD §8 + this run)
1. **Lockstep deploy.** Ship this frontend together with SP1+SP2 in one release window. No `/simplify*` aliases kept — nothing is in production. You own release ordering.
2. **CORS `Access-Control-Expose-Headers: X-Session-Id, X-Trace-Id`.** Confirm the backend/SP4/SP2 CORS config (active `fixing-cors` branch) lists both headers so Task 8's reads work.
3. **`VITE_DEFAULT_VERSION` env var removed (PRD §8.3).** `DEFAULT_VERSION` is now a plain `'v1-2'` constant in `config.ts`. Remove the `VITE_DEFAULT_VERSION` entry from any `.env`, `.env.local`, `.env.production` files in the frontend if they exist. Record this cleanup if done.
4. **`doc_type` literal changed to `'care_plan'` (Task 7).** Coordinate with SP1: the backend `CarePlan` model and LLM prompt must also emit `doc_type: 'care_plan'` (not `'appointment_note'`). Lockstep change.
