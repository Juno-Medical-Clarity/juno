# Code Run — 2026-06-26 06:15 — 01-html-file-support (all tasks)
Branch: feature/html-file-support   Base: feature/datasets

## Tasks completed

| Task | Name | Status | Commit | Files | Tests |
|------|------|--------|--------|-------|-------|
| 1 | Add beautifulsoup4 to requirements.txt | done | 9cc4b2d | backend/requirements.txt | import verified |
| 2 | Extend ALLOWED_EXTENSIONS with html and htm | done | 8e3dd27 | backend/utils/constants.py | python assertion passed |
| 3 | Create utils/html.py with extract_text_from_html | done | f1e6fa5b | backend/utils/html.py | manual smoke tests passed |
| 4 | Add HTML/HTM dispatch in _extract_text_from_bytes | done | 52cba99f | backend/routes/care_plan.py | manual smoke tests passed |
| 5 | Update error message and merge candidates for HTML | done | bab9963e | backend/routes/care_plan.py | grep verified |
| 6 | Update frontend to accept HTML files | done | 302afe4b | frontend/src/pages/care-plan/CarePlanPage.tsx | npm run build ✓ |
| 7 | Add unit tests for extract_text_from_html | done | 849254e1 | backend/tests/utils/test_html.py | 7 passed |

**Full backend suite:** 348 passed, 0 failed, 1 warning (PyPDF2 deprecation — pre-existing)

## Deviations from the PRD/TASKS

- Task 7: pyproject.toml `addopts` includes `--cov=. --cov-report=term-missing` which requires `pytest-cov` (not installed in venv). Tests were run with `--override-ini` to suppress coverage flags. All 7 tests pass. This is a pre-existing environment gap unrelated to this task. The full 348-test suite was also confirmed green with `--override-ini`.

## Follow-ups / new issues discovered

- None. All changes are additive and strictly scoped.

## Still requires you (from PRD §8 + this run)

- None. PRD §8 states no manual steps required. `pip install -r requirements.txt` picks up `beautifulsoup4` automatically on next deploy.

## Manual verification checklist (from PRD §7.3)

Run these after deployment:
- Upload a `.html` file via drag-and-drop UI — should pass frontend validation and be accepted by backend
- Upload a `.htm` file — same result
- Upload a `.html` file alongside a `.pdf` — both processed; combined PDF in GCS includes content from both
- Upload an unsupported type (e.g. `.png`) — error message reads "...Use PDF, TXT, DOCX, or HTML."
- HTML file with `<script>` and `<style>` blocks — script/style text must not appear in extracted output or final care plan

---

## Post-review fix (Opus code review)

Opus review findings and resolutions:

| Finding | Severity | Resolution |
|---------|----------|------------|
| Inline text fragmentation: `get_text(separator="\n")` splits inline tags (`<b>`, `<i>`, etc.) onto separate lines | Medium | Fixed in commit 9d6c9f8b — added inline tag unwrapping (`tag.unwrap()` on inline tags) + `soup.smooth()` before extraction |
| Hidden CSS content (`display:none`) reaches LLM — prompt-injection vector | Low/Medium | Deferred — CSS-layout-aware filtering is out of scope; recommend downstream prompt hardening |
| No test covering inline text preservation | Low | Fixed in commit 9d6c9f8b — added `test_extract_text_preserves_inline_formatting` (now 8 tests total) |
| `beautifulsoup4` unpinned in requirements.txt | Nit | Accepted — consistent with `python-docx`, `textstat`, and other unpinned deps in this file |

**Post-fix state:** 349 backend tests pass, 8 HTML unit tests pass (including new inline test).
