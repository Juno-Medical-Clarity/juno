# Code Review — SP02 (02-constants-and-root-layout)

**Date:** 2026-06-30
**Reviewer:** automated code review (Opus 4.8)
**Branch:** users/tejitpabari/llm-code-check (reviewed in place, no checkout/commit)

## Overall Verdict: PASS-WITH-ISSUES

SP02 is implemented correctly and completely. All 16 tasks are verifiable in the current
tree, the namespaced `Constants` design is in place, the observability package move is done,
`config.py` is deleted, the backend root is clean (only `app.py`), and the targeted test
suites pass (**38 passed**). One PRD-listed migration (`saved_outputs.py` signed-URL TTL) was
never assigned a task and remains an inline literal. Several files were subsequently relocated/
edited by later sub-projects (SP03+), so the live tree differs cosmetically from the SP02 spec;
those are not SP02 defects but are noted for transparency.

---

## Per-Task Checklist

| Task | Requirement | Result |
|---|---|---|
| 02.1 Rewrite constants.py namespaced + shims | Namespaced nested classes + flat shims | VERIFIED — `utils/constants.py` has all namespaces, `MAX_TOKENS_LONG_FORM=65536`, `AthenaSourceKind(StrEnum)`, shims at bottom. (See note 1 — later SPs trimmed/extended it.) |
| 02.2 Add `utils/env.py` accessor + tests | `get_env(key, default, *, required)` + 6 tests | VERIFIED — `utils/env.py` matches spec exactly; `tests/utils/test_env.py` present, passes. |
| 02.3 Migrate athena_client to Constants.Athena.* | Remove literals, use Constants | VERIFIED — now at `services/external_api/athena_client.py` (moved by later SP); all 8 sites use `Constants.Athena.*`, zero literals. |
| 02.4 Migrate llm.py | model/temperature/token literals → Constants.Llm.* | VERIFIED — `utils/llm.py:44,60,117` all use Constants; only remaining "gemini-1.5-pro" is a docstring (line 10). |
| 02.5 Migrate pipeline.py | temps + `65536` → Constants | VERIFIED — `care_plan/v1_2/pipeline.py:85,94,118,134,138` all use `Constants.Llm.*` incl. `MAX_TOKENS_LONG_FORM`. |
| 02.6 Migrate gcs_datasets.py | GCS_PRESET_PREFIX, TEMP_BASE → Constants.Storage.* | VERIFIED — literals gone; `TEMP_BASE: Path = Path(Constants.Storage.TEMP_BASE)` derives from constant. (Minor deviation, note 2.) |
| 02.7 Migrate marker.py DIM_* | → Constants.Observability.* | VERIFIED — `utils/markers/marker.py:50`, `context.py:33,37` use `Constants.Observability.DIM_*`; no literal defs. |
| 02.8 Migrate worker.py deadlines | → Constants.Deadlines.* | VERIFIED — `routes/worker.py:197,199`; module-level literals removed. |
| 02.9 Migrate care_plan_jobs/batch_jobs timeouts | env reads + "10 MB" → Constants | VERIFIED — `care_plan_jobs.py:142`, `batch_jobs.py:97` use `Constants.Deadlines.*`; no `os.environ.get` timeout reads; no "10 MB limit" literal. |
| 02.10 Remove aliases in care_plan/batch | delete re-aliases | VERIFIED — no `RESULT_SENTINEL=/MAX_AGGREGATE_FILE_BYTES=/CARE_PLAN_VERSION=/_UPLOAD_PREFIX=` in `routes/care_plan.py`; `batch.py` renamed `batch_utils.py` (later SP) with no `MAX_BATCH_RUNS` alias. |
| 02.11 models input/grading re-exports | `= Constants.Schema.*` | VERIFIED — `models/input.py:13`, `models/grading.py:10` re-export from Constants; no "1.0" literals. |
| 02.12 Migrate logging_config/telemetry | _EXTRA_KEYS + service-name → Constants | VERIFIED — values sourced from `Constants.Observability.*`; `juno_logger.py:26` uses `Constants.EnvVars.K_SERVICE` with `"juno-backend"` default preserved per PRD §4b. |
| 02.13 Create observability package + move | move 2 modules, update imports | VERIFIED — `observability/{__init__,logging_config,telemetry}.py` exist; root copies deleted; `app.py:14`, `care_plan.py:44` updated; `_build_version` path uses `parent.parent`. No stale `from logging_config/telemetry` importers remain. |
| 02.14 Delete config.py | remove dead file | VERIFIED — `backend/config.py` absent; `find_spec('config')` test passes. |
| 02.15 test_constants.py | namespace + shim tests | VERIFIED — file present, passes (adjusted to match final constants — note 1). |
| 02.16 integration tests | config-deleted + root-clean | VERIFIED — `test_dead_code_removed.py` passes; `allowed={"app.py"}` reflects SP01 moving error_codes into `errors/`. |

**Test result:** `pytest tests/utils/test_constants.py tests/utils/test_env.py tests/utils/test_telemetry_version.py tests/utils/test_logging_config.py tests/integration/test_dead_code_removed.py` → **38 passed**. `constants.py` and `env.py` at 100% line coverage.

---

## PRD Compliance Summary

| PRD Goal (§2) | Status |
|---|---|
| 1. Namespaced nested `Constants` | MET |
| 2. Migrate scattered constants; zero stray hits for the named literals | MET — backend `*.py` (excl. constants.py/docstrings) returns zero hits for the grep set |
| 3. Remove care_plan re-aliases + batch unused alias | MET |
| 4. Define `AthenaSourceKind(StrEnum)` in Constants | MET (later SPs added more members; defining requirement satisfied) |
| 5. Delete `backend/config.py` | MET |
| 6. Move logging_config + telemetry to `observability/`, update import sites | MET |
| 7. Only `app.py` (+ SP01-owned error catalog) at backend root | MET — root has only `app.py`; error_codes lives in `errors/` (SP01) |

Non-goals respected: no behavior/signature/env-var changes; `AthenaSourceKind` consumers not migrated; no schema/frontend changes.

---

## Prioritized Issues

### P2 — Minor / incomplete
1. **`routes/saved_outputs.py:225` — signed-URL TTL never migrated.**
   PRD §4b migration table (line 264) explicitly lists `expiration=timedelta(minutes=30)` →
   `Constants.Storage.SIGNED_URL_TTL_MIN`, but **no TASKS.md task covered it**, so it remains an
   inline literal. `Constants.Storage.SIGNED_URL_TTL_MIN = 30` is defined but unused. This is a
   genuine PRD-goal gap (already acknowledged in the run summary as a follow-up). Low impact
   (value identical), but the PRD's "migrate every scattered constant" goal is not 100% met.

### P3 — Informational (not SP02 defects; branch progressed past SP02)
2. **`gcs_datasets.py` keeps a module-level `TEMP_BASE`** derived from the constant rather than
   inlining `Path(Constants.Storage.TEMP_BASE)` at each callsite as the task text literally
   described. The hardcoded literal is gone and the value comes from Constants, so the intent is
   satisfied; passes the task's own acceptance grep.
3. **Live `constants.py` differs from the SP02 spec** because later sub-projects (SP03+) edited it:
   `SUMMARY_SCHEMA_VERSION_1_3/1_4` and `ALLOWED_VERSIONS` (and their shims) were removed,
   `Schema.CARE_PLAN_VERSION` added, `AthenaSourceKind` gained 5 members, and the
   `DATASETS_BUCKET_*` flat shims were dropped. `test_constants.py` was updated to match (it no
   longer asserts the removed members). Verified no orphaned references to any removed shim/member
   remain in `backend/` — tree is internally consistent and green.
4. **Files relocated by later SPs:** `utils/athena_client.py` → `services/external_api/athena_client.py`;
   `routes/batch.py` → `routes/batch_utils.py`. SP02's edits to those files are intact at the new paths.

---

## Conclusion

SP02's deliverables are fully present and correct in the tree, with passing tests and a clean
backend root. The only substantive gap is the un-migrated `saved_outputs.py` signed-URL TTL,
which was dropped between PRD and TASKS rather than implemented incorrectly. Verdict:
**PASS-WITH-ISSUES** (1 real issue, P2).
