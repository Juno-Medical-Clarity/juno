# Code Review — SP08 Cross-Cutting Error-Handling + Metrics + Dedup Sweep
**Reviewer:** automated code review · **Date:** 2026-06-30 · **Branch:** users/tejitpabari/llm-code-check

## Overall Verdict: PASS-WITH-ISSUES

All 14 tasks are implemented and the SP08 commits (b9daaf17 … f57c31c4) are present in history. The targeted SP08 test set (106 tests) passes and the full backend suite is green (454 passed, 0 failed). One genuine deviation remains (residual `_score_or_none` in `care_plan.py`), and the run summary misreports the dedup grep as clean when it is not.

---

## Per-Task Checklist

| Task | Summary | Result |
|---|---|---|
| 08.1 | `derive_output_name` in `utils/output_helpers.py` | VERIFIED — `backend/utils/output_helpers.py:4`, full priority ladder + try/except, 60-char caps. |
| 08.2 | Migrate call sites; drop `_derive_name`/`_output_name` | VERIFIED — `worker.py:327` uses `derive_output_name`; `_derive_name` removed; `_output_name` already gone (SP06). Greps clean for both names. |
| 08.3 | `score_text_safe`; drop dup safe-scorers | ISSUE — `score_text_safe` added (`utils/scoring.py:307`); `_score_safe` removed from `grading.py`. BUT `_score_or_none` still defined in `care_plan.py:25` and called at `:252-253` (now a thin delegating wrapper). Acceptance grep NOT clean. |
| 08.4 | `get_gcs_bucket` in `utils/gcs_helpers.py`; migrate | VERIFIED — `gcs_helpers.py:9`; `saved_outputs.py:177,220` and `care_plan.py:64,177` use it; `grep gcs.Client( routes/` returns nothing. |
| 08.5 | `enqueue_job_safe`; migrate `care_plan_jobs.py` | VERIFIED — `cloud_tasks.py:66`; `care_plan_jobs.py:130-145` uses the env guard + `if err := enqueue_job_safe(...)` shape. `batch_jobs.py` left to SP06 per scope. |
| 08.6 | `_extract_bearer_token`; de-dup decorators | VERIFIED — `firebase.py:67`; both `verify_firebase_token` and `require_admin` call it (`:85`, `:117`). |
| 08.7 | `FirestoreError` from 5 job wrappers | VERIFIED — sentinel `firebase.py:25`; all 5 wrappers (`:201-261`) try/except → `raise FirestoreError(...) from exc`. |
| 08.8 | Expand Markers registry | VERIFIED — `markers.py` has `Grading.Route`, `Worker.*`, `SavedOutputs.*` (6), `Batch.CreateJobs`, `Firestore.JobWrite`, `Athena.ApiCall`. Smoke prints all six names. |
| 08.9 | Wrap `saved_outputs.py` + markers | VERIFIED — all 6 handlers wrapped in `Markers.SavedOutputs.*.execute(_run)` with `make_error_response` 500s and `scope.mark_failed()`. |
| 08.10 | Wrap `admin.py` + `grading.py` + Grading.Route | VERIFIED — `admin.py:23-70` full-body try/except; `grading.py:70-74` wraps Firestore update; handler under `Markers.Grading.Route` (`:94`). |
| 08.11 | `RenameOutputRequest`/`GradingRequest`; drop ladders | VERIFIED — `models/saved_outputs.py` (Pydantic v2 `field_validator`); `models/grading.py:71` `GradingRequest` with `ConfigDict(strict=True)`. Both routes parse via the models; no isinstance ladder remains in `rename_saved`. |
| 08.12 | Remove legacy `input_pdf_gcs` fallback | VERIFIED — `saved_outputs.py:174,211` use single-leg expr; `grep input_pdf_gcs routes/ utils/` only matches the unrelated `input_pdf_gcs_uri` job-payload field in `care_plan_jobs.py`. |
| 08.13 | Wrap `execute_job` in `Worker.JobExecute` | VERIFIED — OIDC/queue checks stay outside (`worker.py:157-163`); `_run` at `:165`, wrapped at `:348`; `scope.mark_failed()` in failure branch (`:335`). |
| 08.14 | Section headers in 3 flat files | VERIFIED — dividers present in `saved_outputs.py`, `firebase.py`, and `jargon_db.py` (4 dividers, comment-only). |

---

## PRD Compliance Summary

- **Goal 1 (error handling):** Met. All Firestore reads/writes in `saved_outputs.py`, `admin.py`, `grading.py`, and the five `firebase.py` wrappers are guarded; routes return structured `make_error_response(INTERNAL_ERROR)` 500s; wrappers re-raise `FirestoreError`.
- **Goal 2 (markers):** Met. Four new namespaces + `Grading.Route` registered and wired into saved-outputs, grading, and worker surfaces.
- **Goal 3 (dedup):** Mostly met. `derive_output_name`, `get_gcs_bucket`, `enqueue_job_safe`, `_extract_bearer_token`, and `score_text_safe` each exist once. The named private `_score_or_none` was retained as a delegating shim rather than deleted (see Issue 1).
- **Goal 4 (Pydantic):** Met. Both `rename_saved` and `run_care_plan_grading` validate via models; isinstance ladder removed.
- **Goal 5 (legacy removal):** Met. `input_pdf_gcs` fallback deleted from both sites.
- **Goal 6 (section headers):** Met across all three files.
- **Non-goals respected:** No pipeline/orchestration rewrites; `jargon_db.py` change is comment-only.

Note: PRD examples import from `utils.error_codes`, but the live codebase uses the `errors` package — consistent throughout the changed files; not a defect.

---

## Prioritized Issues

### P2 (Minor) — `_score_or_none` not deleted from `care_plan.py`; run summary misreports dedup grep
- **Where:** `backend/routes/care_plan.py:25-27` (definition) and `:252-253` (call sites).
- **What:** Task 08.3 step 4 and its acceptance criterion (`grep -rn "_score_or_none\|_score_safe" backend/routes` returns nothing), plus Definition-of-Done #3, require the function to be removed and call sites switched to `score_text_safe`. Instead it was kept as a thin wrapper (`return score_text_safe(text, label)`) with comment "Thin wrapper so tests can patch scoring without touching the import."
- **Impact:** Low. The duplicate scoring *logic* is genuinely centralized (the wrapper delegates), so the substantive dedup goal holds. However the acceptance grep is NOT clean, and `code-2026-06-30-1500.md` line 43 explicitly claims the grep returns "0 results," which is false. This should have been recorded as a deliberate deviation.
- **Fix options:** (a) inline `score_text_safe` at `care_plan.py:252-253` and delete `_score_or_none`, or (b) update the task/run-summary to record the wrapper as an accepted deviation with rationale.

### Informational
- The run summary (line 32) claims "5 pre-existing test failures in `test_care_plan_markers.py` / `test_save_output.py`." As of this review the full suite passes (454 passed, 0 failed) — those failures were resolved by later sub-projects and are not an SP08 concern.
- `ruff check` on all changed files: clean.
- `worker.py` uses `job.input_source_filename` (JobDoc model attribute) rather than the PRD's `job_doc.get("input_source_filename", "")`; functionally equivalent given SP05's typed JobDoc.

---

## Verification Evidence
- Marker smoke: `worker.job_execute saved_outputs.list batch.create_jobs firestore.job_write athena.api_call grading.route`
- SP08 targeted tests: 106 passed.
- Full backend suite: 454 passed, 0 failed.
- `grep gcs.Client( routes/` → empty; `grep input_pdf_gcs` → only unrelated `input_pdf_gcs_uri`.
- `ruff check` → all checks passed.
