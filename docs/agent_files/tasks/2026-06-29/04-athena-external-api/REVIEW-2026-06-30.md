# Code Review — SP04 (04-athena-external-api)

**Reviewer:** automated code review
**Date:** 2026-06-30
**Branch:** users/tejitpabari/llm-code-check (not switched, not committed)

## Overall Verdict: PASS-WITH-ISSUES

All 9 tasks are implemented and verified against actual source. The full PRD goal set is satisfied: the client is relocated to `services/external_api/`, `AthenaAPIError` inherits `JunoError`, typed Pydantic models are wired into every parse, all tunables resolve through `Constants.Athena.*`, the `Markers.Athena` leaf is added and wraps all three operations, the dead batch method is gone, and the legacy module is deleted. Targeted tests pass (32 passed). The issues found are minor/cosmetic deviations, all functionally benign; several are explicitly documented in the run summary.

## Per-Task Checklist

| Task | Status | Notes |
|---|---|---|
| 04.1 services skeleton | VERIFIED | `backend/services/__init__.py` + `backend/services/external_api/__init__.py` exist and import cleanly. |
| 04.2 AthenaAPIError(JunoError) | VERIFIED | `models/external_api/athena_errors.py:5` inherits `JunoError`; default `ErrorCode.ATHENA_API_ERROR`; preserves `.status_code`/`.body`; `super().__init__(code, detail=...)` matches `JunoError.__init__` (errors/exceptions.py:59). |
| 04.3 AthenaClient relocation | VERIFIED | `services/external_api/athena_client.py` — no class tunables, typed-model parses, Markers wrapping, scope-aware retries, no `BATCH_*`/`fetch_items`, singleton at line 140. |
| 04.4 Markers.Athena leaf | VERIFIED | `utils/markers/markers.py:41-49` — GetToken/FetchEncounterSummary/FetchClinicalDoc with correct metric names. |
| 04.5 worker.py import | VERIFIED | `routes/worker.py:231` imports `from services.external_api import athena_client, AthenaAPIError`; no `utils.athena_client` refs remain. |
| 04.6 __init__ re-exports | VERIFIED | `services/external_api/__init__.py` re-exports the three symbols with `__all__`. |
| 04.7 delete legacy module | VERIFIED | `utils/athena_client.py` deleted; repo-wide grep for `utils.athena_client` empty; ModuleNotFoundError confirmed. |
| 04.8 move+rewrite tests | VERIFIED (minor deviation) | Suite at `tests/services/external_api/test_athena_client.py`; all required typed-model/error-code/constants/markers tests present; dead-method tests removed. Deviation: the two `tests/services*/__init__.py` package markers from step 1 were intentionally NOT created (importlib conflict with prod `services` pkg) — tests still pass. |
| 04.9 dead-module regression | VERIFIED | `tests/integration/test_dead_code_removed.py:64` asserts `utils.athena_client` is gone. |

## PRD Compliance Summary

- Goal 1 (relocate client + singleton): MET.
- Goal 2 (AthenaAPIError → JunoError, ATHENA_AUTH_FAILED on token fail, ATHENA_RATE_LIMIT_ERROR on exhaustion): MET — see athena_client.py:53 (auth), :88-92 and :105-109 (rate limit).
- Goal 3 (typed models wired, no raw `.get()`): MET — grep for `.get("summaryhtml"`/`.get("documentdata"` under services/models returns nothing.
- Goal 4 (all tunables via Constants.Athena): MET.
- Goal 5 (Markers.Athena with status_code/retries/source_kind): MET — dimensions added in `_get` and `_do` closures.
- Goal 6 (delete fetch_items_with_rate_limit + tests): MET.
- Goal 7 (worker import update): MET.
- Goal 8 (test move + patch targets + new tests): MET (with the __init__ deviation above).
- Non-Goals respected: OAuth/retry/strip-html logic unchanged; `extra="allow"` retained per Q4; worker source-kind literals untouched.

## Prioritized Issues

**LOW-1 — Test package markers not created (deviation from TASKS 04.8 step 1).**
`tests/services/__init__.py` and `tests/services/external_api/__init__.py` do not exist. The run summary documents this as an intentional deviation (they collide with the production `services` package under pytest importlib mode). Tests pass without them, so functionally fine, but it diverges from the written task and from the PRD §7a instruction to create them.

**LOW-2 — Residual `BATCH_SIZE`/`BATCH_SLEEP_S` still live.**
`utils/constants.py:60-61` still defines `Constants.Athena.BATCH_SIZE`/`BATCH_SLEEP_S`, and `tests/utils/test_constants.py:33-34` still assert them. These belonged to the deleted `fetch_items_with_rate_limit` (PRD §4c wants them gone). They now live in SP02-owned constants rather than the client module, so SP04 correctly did not copy them; cleanup is an SP02 follow-up. The run summary flags this. Dead config surface remains until then.

**LOW-3 — Cosmetic deviations from PRD-spec'd literals (no functional impact).**
- Import source: `from errors import ErrorCode, JunoError` (athena_errors.py:2, athena_client.py:19) rather than the PRD/TASKS placeholder `utils.pipeline_errors` + `error_codes`. This is the actual consolidated SP01 location (`backend/errors/`), so it is correct per Q7's "bind to real location" guidance.
- `DIM_STATUS_CODE` resolved as `Constants.Observability.DIM_STATUS_CODE` (athena_client.py:50,86,100) instead of importing the symbol from `utils/markers/marker.py`. Same string value ("StatusCode"); behavior identical.
- Constant names `HTTP_TIMEOUT_TOKEN_S`/`HTTP_TIMEOUT_GET_S`/`MAX_RETRIES` and `AthenaSourceKind.ATHENA_ENCOUNTER`/`ATHENA_CLINICAL_DOC` differ from the PRD's illustrative names; they match the actual SP02 constants. Correct.
- `athena_errors.py` omits `from __future__ import annotations` (not needed; no forward refs) and uses detail format `status={} body={}` vs PRD's `status={}: {}`. Cosmetic.

## Note (not an SP04 defect)

`routes/worker.py:246` uses `build_error_data_from_exc(exc)` rather than the `build_error_data(ErrorCode.ATHENA_API_ERROR, ...)` + `exc.status_code` path the PRD described as "unchanged." That handler was changed by a sibling SP's unified-error work, not SP04 — SP04 touched only the import line (verified: grep clean, single import line at 231). It functions correctly because `AthenaAPIError` now inherits `JunoError`. No regression attributable to SP04.
