# SP09 Dead-Code Purge + Linter — Code Review (2026-06-30)

Reviewer: automated code review against PRD.md + TASKS.md
Branch: users/tejitpabari/llm-code-check (not switched, not committed)

## Overall verdict: PASS

All 10 tasks are implemented and verified against actual source. `ruff check . --config pyproject.toml` exits 0 ("All checks passed!"), the affected test suites pass (24 passed across the three directly-impacted test files), every dead-symbol verification grep returns zero project-code matches, the pre-commit config and README Linting section exist. One low-priority advisory only (CI/cloudbuild ruff step), which the PRD explicitly treats as optional.

---

## Per-task checklist

| Task | Result | Evidence |
|---|---|---|
| 09.1 Install ruff + configure pyproject.toml | VERIFIED | `backend/requirements-dev.txt:5` `ruff>=0.4,<0.5`; `backend/pyproject.toml:9-16` `[tool.ruff]` with line-length 120, target py312, exclude tests, `select=["F"]`. `ruff check` exits 0. `noqa: F401` present at `models/__init__.py:11`. |
| 09.2 Pre-commit ruff hook | VERIFIED | `/root/projects/juno/.pre-commit-config.yaml` matches PRD spec exactly (ruff-pre-commit v0.4.10, args `--config backend/pyproject.toml`, files `^backend/.*\.py$`). |
| 09.3 Delete ALLOWED_VERSIONS, SUMMARY_SCHEMA_VERSION_1_3/1_4 | VERIFIED | Absent from `utils/constants.py`. Grep returns zero (excl. constants.py). `SUMMARY_SCHEMA_VERSION_1_2` correctly retained at `utils/constants.py:13`. |
| 09.4 Delete dead dataset bucket constants | VERIFIED | `DATASETS_BUCKET_NAME_DEFAULT`, `DATASETS_BUCKET_ENV_VAR`, `DATASETS_BUCKET_NAME_ENV_VAR` all absent. Grep zero. (Note: constants.py was restructured into nested namespaces by upstream SPs; the differently-named `EnvVars.DATASETS_BUCKET`/`DATASETS_BUCKET_DEFAULT` are new, unrelated symbols.) |
| 09.5 Delete InputFile, FileInput, BatchDatasetInput | VERIFIED | `models/input.py` contains only `TextInput`, `DocIdInput`, `Input` union (TextInput, DocIdInput), plus new `ResolvedInput`. No TYPE_CHECKING/`Any` imports. `models/__init__.py` exports cleaned. Grep zero in project code (only .venv third-party `*FileInput*` hits, irrelevant). |
| 09.6 Update test_input_metrics.py | VERIFIED | No `BytesIO`, `FileStorage`, `InputFile`, `FileInput`, `BatchDatasetInput` references remain. `tests/models/test_input_metrics.py` passes. |
| 09.7 Delete ADMIN_BLUEPRINTS + all_blueprints alias | VERIFIED | `routes/__init__.py` has no `ADMIN_BLUEPRINTS`, no `all_blueprints`, no stale inline comment; `admin_bp` is in `API_BLUEPRINTS`. Grep zero repo-wide. Test files updated (`test_route_rewire.py` passes). |
| 09.8 Remove import enum from grading.py | VERIFIED | `models/grading.py:1` is now `from datetime import datetime, timezone`; `import enum` gone. Grep `^import enum` zero. |
| 09.9 Stale comments / docstrings | VERIFIED | `routes/datasets.py:36` -> "On-demand GCS fetch unavailable"; `utils/jargon_db.py:1-6` docstring rewritten (no stale DB sentence); `models/care_plan/care_plan.py:8` docstring updated to version-agnostic wording; `utils/constants.py:34` RESULT_SENTINEL comment is "SSE stream result sentinel". |
| 09.10 Document ruff in backend/README.md | VERIFIED | `backend/README.md:117` "## Linting" section with `ruff check` command and pre-commit install instructions. |

---

## PRD compliance summary

- Goals 1-8 all satisfied. Ruff installed with minimal F ruleset (Goal 1), pre-commit hook added (Goal 2), dead constants/models/blueprints/import removed (Goals 3-6), stale comments rewritten (Goal 7), README documented (Goal 8).
- Non-goals respected: only F rules enabled (no E/W/ANN/UP); upstream-owned dead code (athena_manifest, batch helpers, etc.) untouched; no frontend changes; no API/Firestore wire-format changes.
- Open questions resolved correctly: Q1 (input models safe to delete post-SP05 — confirmed no `job.py` reference), Q2 (all three dataset constants deleted), Q5 (`SUMMARY_SCHEMA_VERSION_1_2` retained), Q6 (`all_blueprints` deleted), Q7 (inline noqa used, no blanket ignore).
- Deviations in code summary are accurate and benign: constants.py restructured by upstream into nested namespaces (TASKS line numbers stale but deletions still achieved); `models/care_plan.py` actually lives at `models/care_plan/care_plan.py`; two pre-existing unrelated test failures fixed inline; test/conftest `all_blueprints` references updated.

---

## Issues (prioritized)

### Low / advisory
1. **CI ruff enforcement not added to `backend/cloudbuild.yaml`.** PRD Q4 was "RESOLVED: add both — pre-commit hook for local dev, document `ruff check` in CI/cloudbuild." Only the pre-commit hook + README documentation exist; `cloudbuild.yaml` has no `ruff` step (`grep ruff cloudbuild.yaml` -> none). This is explicitly listed as optional/"still requires you" in the run summary, and PRD §4A states a README/CI note is sufficient, so it is not a blocker — but the durable CI backstop the PRD favored is absent. Recommend a follow-up to add a `ruff check backend/` build step.

### Informational (not defects)
- The PRD/TASKS verification grep for B4 (`InputFile|FileInput|...`) does not exclude `.venv`, so a literal run surfaces dozens of unrelated third-party matches (google/genai, vertexai, pygments). Project-code matches are zero. Worth tightening the documented grep with `| grep -v .venv` to avoid false alarms in future audits.
- TASKS.md step-level line numbers for `utils/constants.py` no longer correspond to the file (restructured upstream). Deletions were still correctly achieved by symbol, not line.

---

## Verification commands run
- `ruff check . --config pyproject.toml` -> All checks passed (exit 0)
- `pytest tests/models/test_input_metrics.py tests/utils/test_constants.py tests/routes/test_route_rewire.py` -> 24 passed
- All five dead-symbol greps from TASKS Verification section -> zero project-code matches
