# SP06 Code Review — 2026-06-30

Reviewer: automated code review (Opus 4.8)
Branch: `users/tejitpabari/llm-code-check` (current HEAD — includes SP07–SP10 follow-on work)
Scope: SP06 — Batch + Job-Creation Route Hardening

## Overall verdict: PASS

Every SP06 task is implemented in the actual source and matches the PRD/TASKS intent. All 24 SP06-targeted tests pass; the full backend suite passes (437 passed) when the one unrelated, pre-existing collection error in `tests/errors/` is excluded. The two test regressions flagged in the run summary (`test_envelope.py`, `test_care_plan_markers.py`) have since been fixed and now pass. The only flagged items are informational / out-of-SP06-scope.

---

## Per-task checklist

| Task | Status | Evidence |
|---|---|---|
| 06.1 — `models/batch_requests.py` (coercion, Selection union, request models) | VERIFIED | `backend/models/batch_requests.py:1-94` matches spec exactly: `_coerce_grading_enabled`, three per-kind models with `model_validator`, discriminated `Selection`, `BatchJobsRequest` (`grading_enabled=False`), `SingleJobRequest` (`grading_enabled=True`). |
| 06.2 — `Markers.Batch` leaf | VERIFIED | `backend/utils/markers/markers.py:54-59` defines `Batch.CreateJobs` (`"batch.create_jobs"`) and `CreateSingleJob` (`"batch.create_single_job"`). Tests assert names. |
| 06.3 — rename `batch.py`→`batch_utils.py`, delete dead helpers | VERIFIED | `backend/routes/batch_utils.py` holds only `batch_bp`, `_batch_timestamp`, `_resolve_requested_runs`. Five dead helpers + `_grading_enabled` gone. `routes/__init__.py` and `batch_jobs.py:11` import from `routes.batch_utils`. grep for old module: clean. |
| 06.4 — rewrite `create_care_plan_batch_jobs` | VERIFIED | `backend/routes/batch_jobs.py:38-198`: `BatchJobsRequest.model_validate`, isinstance partition (no `ATHENA_KINDS`), `_resolve_requested_runs([s.model_dump()...])`, `MAX_BATCH_RUNS` check, `scope.add_many` with `source_kind`, `JobDoc.for_gcs_dataset`/`for_athena`, inner+outer try/except, 202 response. No inline job-doc dicts, no inline coercion. |
| 06.5 — rewrite `create_care_plan_job` | VERIFIED | `backend/routes/care_plan_jobs.py:29-153`: `SingleJobRequest.model_validate` with form>json precedence + None-drop, `JobDoc.for_single`, `Markers.Batch.CreateSingleJob`, local 400 catch nested in outer 500 catch. `_grading_enabled_from_request` import removed. |
| 06.6 — delete `_grading_enabled_from_request` | VERIFIED | grep `_grading_enabled_from_request` across `backend/`: zero hits. |
| 06.7 — section-header comments | VERIFIED | `batch_jobs.py` has 6 `# ── …` headers in spec order (Parse/Partition/Resolve GCS/Check count/GCS docs/Athena docs). |
| 06.8 — frontend `input_source_kind` discriminator | VERIFIED | `frontend/src/types/datasets.ts:7-12` adds `input_source_kind: "gcs_dataset"`; `PresetDataCard.tsx:38` emits it; `CarePlanPage.tsx:96` sets athena kind; `api/jobs.ts:11` types athena literals. |
| 06.9 — backend tests batch_jobs (7) | VERIFIED | `tests/routes/test_batch_jobs.py` has all 7 required tests + extras; pass. |
| 06.10 — backend tests care_plan_jobs (3) | VERIFIED | `tests/routes/test_care_plan_jobs.py` has the 3 required tests; pass. |
| 06.11 — markers + batch_utils surface tests | VERIFIED (with note) | `tests/utils/test_markers.py` (2 marker tests) + `tests/routes/test_batch_utils.py` (importable + `test_dead_helpers_removed`) pass. The "410 stub still returns 410" sub-test (step 3) is absent — see Issue #2. |

---

## PRD compliance summary

- **Goals 1–9: all met.** Pydantic `BatchJobsRequest`/`SingleJobRequest`, discriminated `Selection` union with per-kind Athena validation, single shared `grading_enabled` coercion (three copies deleted), outer try/except → `INTERNAL_ERROR` 500 on both handlers, `Markers.Batch` instrumentation with the prescribed dimensions (+ bonus `source_kind`), `JobDoc.for_*` factory substitution for all three inline dicts, `Constants.Athena.AthenaSourceKind` replacing `ATHENA_KINDS`, `batch.py`→`batch_utils.py` rename with dead-helper removal and import rewiring.
- **Non-goals respected.** No changes to `_resolve_requested_runs`/`_batch_timestamp` logic, multipart path preserved, no new `ErrorCode` values, `enqueue_job` call contract unchanged.
- **External contract:** request/response shapes unchanged except the planned `input_source_kind: "gcs_dataset"` discriminator (Q3 → Task 06.8), implemented end-to-end.
- **Documented deviations (acceptable):** `JobDoc.for_athena`/`for_gcs_dataset` are called with individual kwargs matching the real SP05 signatures rather than the illustrative `selection=sel`/positional forms in the PRD; `to_firestore()` is called before `create_job_doc(payload=...)`. Both are explicitly sanctioned by the PRD ("adjust the call site to match the upstream signature").

---

## Prioritized issues

All findings are informational / out-of-SP06-scope. No SP06-scoped defects.

1. **[Low / out-of-scope] Full backend suite collection is broken by an unrelated `tests/errors` package-name collision.** Running `pytest backend -q` (the PRD §Verification command) aborts at collection with `ModuleNotFoundError: No module named 'errors.test_exceptions'` (`tests/errors/test_codes.py`, `tests/errors/test_exceptions.py`) — a pytest prepend-import-mode clash between the top-level `errors/` package and `tests/errors/`. This is SP01 territory, not touched by SP06, and was introduced after the SP06 run (the run summary recorded the full suite collecting fine at SP06 time). Excluding `tests/errors`, the suite is green: 437 passed. Worth a follow-up so the documented verification command works again.

2. **[Info] Task 06.11 step 3 "GET/POST /care_plan/batch still returns 410" test is absent.** `tests/routes/test_batch_utils.py` contains the importable/dead-helper assertions but no 410 test. This is consistent with HEAD state: a later SP07 commit (`5e2471dd`) deleted the 410 stub `care_plan_batch_sse_deprecated` (its removal is SP07's owned scope per PRD §3 Non-Goals). At SP06's own commit (`70d047ca`) the stub and its correct docstring were present. Not an SP06 defect at HEAD; noted only because the TASKS line item is no longer satisfiable.

### Resolved (no action)
- Run summary's two flagged regressions (`tests/models/test_envelope.py::test_model_consuming_routes_import_without_removed_aliases`, `tests/utils/test_care_plan_markers.py::test_batch_passes_is_batch_true_and_source_kind`) now pass at HEAD — fixed by follow-on commits.
