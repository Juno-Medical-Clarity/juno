# Code Review — SP03 Models Folder Reorganization

**Reviewer:** automated code review
**Date:** 2026-06-30
**Branch:** users/tejitpabari/llm-code-check (reviewed working tree; not switched, not committed)
**Scope:** `backend/models/` reorg, `external_api` extraction, `ResolvedInput` relocation.

---

## Overall Verdict: PASS-WITH-ISSUES

The reorganization is functionally complete and correct. All file moves, deletions, re-exports, and the `ResolvedInput` conversion are in place. Smoke test passes and `CarePlan._registry` self-registration fires. Affected test suites pass (120 passed across `tests/models/`, `tests/care_plan/`, `tests/services/external_api/`). One genuine documentation miss (PRD Goal 5 not satisfied) and one minor non-canonical-import nit prevent a clean PASS.

---

## Per-Task Checklist

| Task | Result | Notes |
|---|---|---|
| 03.1 Delete dead `athena_manifest.py` + test | VERIFIED | `models/athena_manifest.py` and `tests/models/test_athena_manifest.py` both absent. `grep -r athena_manifest` returns zero (.pyc artifacts only). |
| 03.2 Extract `AthenaAPIError` to `models/external_api/athena_errors.py` | VERIFIED (with documented deviation) | File exists. Class inherits `JunoError` + `ErrorCode.ATHENA_API_ERROR`, not `Exception` — SP01 had already landed; deviation is documented in the run summary and is correct. `status_code`/`body` attributes preserved. No class def remains in client code. |
| 03.3 Move Athena models to `external_api/athena_models.py` | VERIFIED | 11 model classes present (`athena_models.py:11–128`); `extra="allow"` preserved on all (Q1 deferred to SP04). `models/athena.py` deleted. `external_api/__init__.py` re-exports all 11 + `AthenaAPIError`. |
| 03.4 Reorganize care-plan models into `models/care_plan/` | VERIFIED w/ ISSUE | All 5 new files exist; old `care_plan.py`/`envelope.py`/`care_plan_versions/` deleted. Relative imports correct (`..base`, `.care_plan`, `..grading`, `..input`, `..metrics`; `...base`/`..care_plan` in versions/v1_2.py). **ISSUE-1: stale docstring NOT properly fixed** (see below). |
| 03.5 Update `models/__init__.py` re-exports | VERIFIED | Imports from new internal paths; `CarePlanV1_2` import present to trigger registration; `ResolvedInput` exported. Public surface differs from PRD's illustrative listing (no `InputFile`/`FileInput`/`BatchDatasetInput`) — but those classes no longer exist in `input.py` (changed by other SPs), so this is not an SP03 regression. |
| 03.6 Move `ResolvedInput` to `input.py` as `JsonModel` | VERIFIED | `input.py:32` `ResolvedInput(JsonModel)` with `combined_pdf_size: float \| None` (Q3 float variant, no `bytes` field). `routes/care_plan.py`: no `dataclass` import; imports from `models.input`; `_resolve_uploaded_files` returns `tuple[ResolvedInput, bytes \| None]` and computes `combined_pdf_size` (`care_plan.py:160–168`). `routes/care_plan_jobs.py:62–65` unpacks tuple and uses `raw_pdf_bytes` for GCS upload. |
| 03.7 Canonical import paths everywhere | MOSTLY VERIFIED | `worker.py:21`, `care_plan/envelope` and `versions/v1_2` consumers use canonical paths. AthenaAPIError consumers import from `models.external_api.athena_errors`. **ISSUE-2: three call sites still use the package re-export** `from models.care_plan import CarePlan` instead of the prescribed canonical `models.care_plan.care_plan`. |

---

## PRD Compliance Summary

| PRD Goal | Status |
|---|---|
| 1. Group care-plan models under `models/care_plan/` with `versions/` | MET |
| 2. Create `models/external_api/` (Athena models + `AthenaAPIError`) | MET |
| 3. Delete `athena_manifest.py` + its test | MET |
| 4. Move `ResolvedInput` into `input.py` as `JsonModel` | MET (Q3 float variant per approved TASKS decision) |
| 5. Fix stale comment in `care_plan.py` line 8 | **NOT MET** — docstring still references a non-existent path |
| 6. Keep `models/__init__.py` re-exports stable | MET |
| 7. Leave `grading.py`/`metrics.py`/`errors.py` in place | MET (all present, untouched) |

API/Firestore shapes unchanged (Goal: no API change) — confirmed; only internal package structure changed. Q1 (`extra="allow"`) and Q2 (naming) left as planned. The athena HTTP client now lives at `services/external_api/athena_client.py` (SP04 territory) and correctly imports `AthenaAPIError` from the new SP03 location — SP03's contract is satisfied.

---

## Prioritized Issues

### ISSUE-1 (Medium) — Stale docstring not actually fixed; still points to a non-existent path
`backend/models/care_plan/care_plan.py:8`
```python
"""Version-agnostic base model for all care plan outputs. The concrete version schema is CarePlanV1_2 in care_plan/v1_2/models.py."""
```
PRD Goal 5 and Task 03.4 step 1 required replacing the stale comment with one pointing to `models/care_plan/versions/`. The implementation rewrote the docstring but it **still references `care_plan/v1_2/models.py`, which does not exist** (confirmed: `backend/care_plan/v1_2/` contains only `pipeline.py` and `prompts/`). The concrete version actually lives at `models/care_plan/versions/v1_2.py`. The exact stale-path problem the task was meant to remove is still present. Fix: point the docstring to `models/care_plan/versions/v1_2.py`.

### ISSUE-2 (Low) — Non-canonical import path retained in three call sites
- `backend/routes/care_plan.py:30` — `from models.care_plan import CarePlan, CARE_PLAN_VERSION`
- `backend/care_plan/v1_2/pipeline.py:36` — `from models.care_plan import CarePlan`
- `backend/tests/models/test_care_plan.py:9` — `from models.care_plan import CarePlan`

Task 03.7 prescribed the canonical `from models.care_plan.care_plan import CarePlan`. These resolve through the package `__init__.py` re-export and work correctly, and PRD §4.D explicitly allows the re-export path, so this is non-blocking. Note that `routes/care_plan.py:30` also pulls `CARE_PLAN_VERSION`, which is only defined on the package `__init__.py` (`care_plan/__init__.py:13`) — not on the `care_plan.py` module — so it cannot use the fully canonical path without also relocating `CARE_PLAN_VERSION`. The run summary already acknowledges this as an accepted future-cleanup item.

---

## Deviations (informational, not defects)
- `models/care_plan/__init__.py` adds a `CARE_PLAN_VERSION` re-export (`:11–15`) beyond the PRD's `__init__` spec. Necessary because `routes/care_plan.py` imports it from the package; benign.
- `AthenaAPIError` inherits `JunoError` (SP01 already merged) rather than the PRD's illustrative `Exception` stub. Correct, documented.
- `models/__init__.py` also exports `JobDoc` and `pipeline_events.*` from later SPs — outside SP03 scope, not a regression.

---

## Verification Performed
- Structure: confirmed all new files exist and all old files (`athena_manifest.py`, `athena.py`, `care_plan.py`, `envelope.py`, `care_plan_versions/`, `test_athena_manifest.py`) are deleted.
- Grep: `athena_manifest` and stale model paths (`models.care_plan_versions`, `from models.envelope`, `from models.athena import`) return zero hits.
- Smoke test: all prescribed imports succeed; `CarePlan._registry == {'1.2': <CarePlanV1_2>}`; `ResolvedInput(... combined_pdf_size=1024.0)` constructs.
- Tests: `pytest tests/models/ tests/care_plan/ tests/services/external_api/` → 120 passed.
