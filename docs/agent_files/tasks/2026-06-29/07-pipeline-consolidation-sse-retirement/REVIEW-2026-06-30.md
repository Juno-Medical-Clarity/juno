# SP07 Code Review — Pipeline Consolidation + SSE Retirement

**Reviewer:** automated code review
**Date:** 2026-06-30
**Branch:** users/tejitpabari/llm-code-check
**Scope:** Verified actual repository code against PRD.md and TASKS.md; did not trust the run summary blindly.

---

## Overall Verdict: PASS

All 11 tasks are genuinely implemented in the source, the PRD goals and acceptance criteria are satisfied, and the SP07-affected test suite passes (51 passed + 2 interface tests). Only three minor cosmetic issues were found — none affect correctness or behavior.

---

## Per-Task Checklist

| Task | Status | Notes |
|---|---|---|
| 07.1 — Create `models/pipeline_events.py` + export from `models/__init__.py` | VERIFIED | All six dataclasses present (`models/pipeline_events.py:16-65`). Exported and added to `__all__` (`models/__init__.py:13-43`). `from models import AdapterResult` works. Note: TYPE_CHECKING import paths differ from the TASKS snippet (`models.care_plan.versions.v1_2`, `models.care_plan.care_plan`) but are correct for the actual repo layout. |
| 07.2 — `iter_steps()` + thin `run()` | VERIFIED | `iter_steps` implemented exactly per spec (`care_plan/v1_2/pipeline.py:149-252`); step 2 non-fatal, step 3/5 fatal `PipelineStepError`, step 4 non-fatal fallback to `simplified`. `run()` is a 6-line delegator (`pipeline.py:254-261`). Duplicate step chain eliminated. |
| 07.3 — `iter_steps` default on ABC | VERIFIED | `CarePlanPipeline.iter_steps` default delegates to `run()` and yields one `PipelineRunResult` / `PipelineStepError(step=None)` (`care_plan/interface.py:32-52`); local import avoids circular import. Docstring updated. `test_pipeline_interface.py` passes (2). |
| 07.4 — Thin adapter `run_care_plan_pipeline` | VERIFIED | Pure adapter, zero step logic (`routes/care_plan.py:192-288`). `wrap_step` records `term_count`/`substitution_count` for step 2 and `input_chars` for step 3; tracing spans wired; grading + pipeline markers preserved. `_sse`, `_sse_error`, `_sse_error_rich`, `care_plan_sse_deprecated`, `RESULT_SENTINEL`, and `json` import all removed. |
| 07.5 — Worker typed-event loop | VERIFIED | `isinstance` dispatch on `AdapterResult`/`AdapterError`/`AdapterStepEvent` (`routes/worker.py:274-285`); result unpacking is `pipeline_result.care_plan/.grading` (`worker.py:305-306`). No `json` import, no `"data: "`, no `RESULT_SENTINEL`. |
| 07.6 — Delete `POST /care_plan/batch` 410 stub | VERIFIED | `care_plan_batch_sse_deprecated` absent from `routes/batch_utils.py`. `_pipeline_for_version` was already removed by SP06 (confirmed by `tests/routes/test_batch_utils.py::test_dead_helpers_removed`), so the annotation-update step was correctly skipped (documented deviation). |
| 07.7 — Rewrite `test_pipeline_executors.py` | VERIFIED | 3 typed-event tests pass; no `_events_from_chunks`, `"__result__"`, or `"data: "` in the file. |
| 07.8 — Update `test_care_plan_markers.py` | VERIFIED | 18 tests pass; source-level marker-string assertions still hold against the rewritten adapter. |
| 07.9 — Update `test_worker.py` | VERIFIED | 11 tests pass with typed-event fakes; no SSE strings/sentinels. |
| 07.10 — Add `test_pipeline_streaming.py` | VERIFIED | 7 tests pass covering ordering, `wrap_step` per-step, step-3/4/5 failure modes, and `run()` delegation. |
| 07.11 — Update `test_route_rewire.py` | VERIFIED | `/care_plan` and `/care_plan/batch` removed from `expected_care_plan_rules`; stale `_pipeline_for_version` assertion removed. 1 test passes. Folded into 07.6 commit (documented). |

---

## PRD Compliance Summary

- Goal 1 (pipeline owns step chain via single canonical entry point): MET — `iter_steps` is the single owner; named `iter_steps` instead of PRD's `run_streaming` (documented rename, applied consistently).
- Goal 2 (thin adapter, zero step logic): MET.
- Goal 3 (worker consumes typed events, no SSE/sentinel parsing): MET.
- Goal 4 (both 410 stubs deleted): MET (`POST /care_plan` removed in 07.4; `POST /care_plan/batch` removed in 07.6).
- Goal 5 (SSE helpers deleted): MET.
- Goal 6 (section headers added): MET with one minor gap (see Issue #1).
- Goal 7 (`interface.py` ABC retained + updated): MET.
- Non-goals respected: no prompt/model/schema/Firestore changes; `RESULT_SENTINEL` constant intentionally retained in `utils/constants.py` for SP08/SP09 sweep (PRD §4f); `batch_dataset` key untouched (PRD §4i).
- Resolved open questions Q1/Q2/Q4/Q7/Q10 all reflected correctly in code.
- SSE-retirement verification grep over `routes/`, `care_plan/`, `models/` returns zero hits.

---

## Prioritized Issues

### Low / Cosmetic (no functional impact)

1. **Missing "Scoring helpers" section header in `routes/care_plan.py`.** PRD §4g lists five headers; only four are present (`routes/care_plan.py:8,54,70,191`). The `_score_or_none` helper (`routes/care_plan.py:25-27`) is also physically placed *inside* the import block (between `import` statements at lines 17-28), which is stylistically awkward though syntactically valid. Cosmetic.

2. **Stale "SSE" reference in a worker comment.** `routes/worker.py:288` still reads `# error_data is a rich error dict from the pipeline SSE stream.` even though the SSE protocol is fully retired. Comment-only; no behavior impact.

3. **`models/pipeline_events.py` drops the inline field comments and `field` import shown in the TASKS snippet.** The TASKS template included explanatory comments on `PipelineRunResult` fields and imported `field`; the implementation omits both. Purely informational — the dataclasses are otherwise identical and correct (`models/pipeline_events.py:24-31`).

---

## Verification Performed

- Read all six modified/new source files in full and confirmed implementations match task specs.
- Ran the SP07 test set: `test_pipeline_streaming.py` (7), `test_pipeline_executors.py` (3), `test_pipeline_schema.py` (7), `test_care_plan_markers.py` (18), `test_worker.py` (11), `test_route_rewire.py` (1), `test_batch_utils.py` (4) → 51 passed; `test_pipeline_interface.py` → 2 passed.
- Grep confirmed zero SSE/`RESULT_SENTINEL`/`json.loads` references in `routes/`, `care_plan/`, `models/`.
- Confirmed `_pipeline_for_version` and both 410 stubs are absent from the codebase.

No source code was modified during this review.
