# Code Run — 2026-06-30 1946 — SP10 (10-frontend-legacy-shim-retirement)
Branch: users/tejitpabari/llm-code-check   Base: main

## Status: STOPPED BEFORE IMPLEMENTATION — no coding agents dispatched

Per the dev-code precondition check, `PRD.md` §9 ("Open Questions & Decisions") for SP10
still lists two items as **[OPEN]**:

- **Q1**: "Are there any Firestore `care_plan_outputs` documents in production that lack the
  `care_plan` top-level key (i.e., the legacy flat shape that `isLegacyShape()` detects)?
  Backend team (SP05/SP07) must confirm before `normalizeOutput.ts` is deleted." — `[OPEN]`
- **Q2**: "Should the `CarePlanJobPage.tsx` component split (§4f) be included in this SP or
  deferred to a follow-on?" — `[OPEN]`

## Discrepancy worth flagging

`TASKS.md` (generated 2026-06-30 05:44, after the PRD's 2026-06-29 23:29 timestamp) contains
a "USER DECISIONS applied" note claiming Q1–Q4 were resolved by the user:
- Q1 → assumed no legacy documents exist; `normalizeOutput.ts` retirement is treated as
  *unconditional*, not gated.
- Q2 → the `CarePlanJobPage.tsx` component split is *included* in this SP.
- Q3/Q4 → already shown `[RESOLVED]` in the PRD itself.

However, **`PRD.md`'s own §9 table was never updated** to mark Q1 and Q2 as
`[RESOLVED]`/`[DEFERRED]` — they still read `[OPEN]` in the source-of-truth PRD. This is
exactly the situation the dev-code preconditions guard against: TASKS.md asserts a
resolution that the PRD (the document of record) does not reflect. Proceeding on TASKS.md's
say-so alone would mean implementing Task 10.4 (delete `normalizeOutput.ts`, gated on Q1) and
any task touching the §4f component split (gated on Q2) without an auditable resolution in
the PRD — including the real-world risk named in Q1 itself: if legacy flat Firestore
documents still exist in production, deleting `normalizeOutput.ts` and switching
`CarePlanJobPage.tsx` to a direct cast would break rendering for those jobs.

## Action taken

Stopped per instructions before reading further into TASKS.md task bodies or dispatching any
coding sub-agents. No files were changed, no commits were made, no worktrees were created.

## Still requires you (from PRD + this run)

- **Reconcile PRD.md §9 with TASKS.md's claimed decisions.** Either:
  (a) update `PRD.md` §9 to mark Q1 and Q2 `[RESOLVED: ...]` or `[DEFERRED]` to match the
  "USER DECISIONS applied" note in TASKS.md (if that note reflects an actual, intentional
  decision), or
  (b) revise `TASKS.md` if the "USER DECISIONS applied" note was generated speculatively and
  the real decision is still pending.
- **Q1 specifically needs a real backend answer**, not an assumption baked into TASKS.md: has
  anyone (SP05/SP07 backend team) actually confirmed zero legacy-shape `care_plan_outputs`
  documents remain in production Firestore? If not confirmed, deleting `normalizeOutput.ts`
  is unsafe regardless of what TASKS.md says.
- **Q2 needs an explicit owner decision** on whether the optional `CarePlanJobPage.tsx` split
  (§4f) is in-scope for SP10 or a follow-on — TASKS.md says "included," PRD says "open."
- Once PRD.md §9 is reconciled (no `[OPEN]` items remain) and matches TASKS.md, re-run the
  dev-code orchestration for SP10 — it can resume cleanly since no commits were made yet.
