# Code Review — SP10 Frontend Legacy-Shim Retirement
**Reviewer:** automated code review (Opus 4.8)
**Date:** 2026-06-30
**Branch:** users/tejitpabari/llm-code-check
**Scope reviewed:** PRD.md, TASKS.md, three code-*.md run summaries, and the actual repository source.

---

## Overall Verdict: PASS

All 7 tasks are implemented and verified against real source. The TS2352 build
regression that the second run summary (`code-2026-06-30-2019.md`) flagged was fixed
by commit `bfb3b55c` (`as unknown as CarePlanInternal`) and is confirmed resolved.

Independent verification performed during this review:
- `npx tsc -b` from `frontend/` → exit 0, zero type errors.
- `npx vitest run` → 22 test files passed, 170/170 tests passed.
- All five targeted greps from TASKS.md Verification → zero matches.
- Deleted files confirmed gone; new view files confirmed present.

Only two low-priority, non-blocking advisory items remain (see Issues). Neither
affects build, tests, or runtime behavior. Both are already documented as
known/out-of-scope in the run summaries.

---

## Per-Task Checklist

| Task | Requirement | Result |
|---|---|---|
| 10.1 Delete dead `JobErrorData` type | Remove type + block comment from `useJobSnapshot.ts`; keep `FirestoreJobError` import (used by `JobDoc.error_data`) | VERIFIED — `useJobSnapshot.ts` has no `JobErrorData`; `import type { FirestoreJobError }` retained (line 5), `JobDoc.error_data: FirestoreJobError | null` (line 13). grep `JobErrorData` → 0 matches. Still exports `JobStatus`, `JobDoc`, `useJobSnapshot`. |
| 10.2 Remove `flush()` TODO from `logger.ts` | Delete TODO paragraph; keep 6-line docstring; keep in-body comment | VERIFIED — docstring is exactly the 6-line block (lines 1–6); grep `flush` in logger.ts → 0 matches; in-body comment preserved (`logger.ts:109`, "any future log ingestion pipeline"). |
| 10.3 Wire `processingIds` from `useJobStatuses` | Import hook; compute `Set` of `not_started`/`processing` ids; replace dead `undefined` stub | VERIFIED — `CarePlanPage.tsx:14` imports hook; `:46–51` computes the Set exactly per spec. grep `TODO: wire SP1` → 0 matches. |
| 10.4 Drop legacy error comment + tighten `retryable` | Update comment; `retryable ?? false`; gate retryable indicator on `errData`/`errorData` | VERIFIED — implemented in extracted `CarePlanJobErrorView.tsx`: comment is "Technical detail string from FirestoreJobError." (`:22`), `retryable = errorData?.retryable ?? false` (`:21`), indicator gated `{errorData && (` (`:57`). grep `legacy ErrorDetail` → 0 matches. `FirestoreJobError.retryable` is non-optional `boolean` (errors.ts:59), so the tightening is type-correct. |
| 10.5 Retire `normalizeOutput.ts` + direct cast | Delete normalizer + its test; remove import; cast `output_data` | VERIFIED — `normalizeOutput.ts` and `normalizeOutput.test.ts` both deleted; grep `normalizeOutput\|normalizeCarePlanOutput\|isLegacyShape` → 0 matches; cast at `CarePlanJobPage.tsx:55` is `(jobDoc.output_data as unknown as CarePlanInternal)` (double-cast fixup — see note). |
| 10.6 Split into error/result views | Create `CarePlanJobErrorView.tsx` + `CarePlanJobResultView.tsx`; make `CarePlanJobPage` a thin orchestrator | VERIFIED — both files exist and are imported (`CarePlanJobPage.tsx:15–16`); page now dispatches to children for completed/error states; inline JSX bodies removed; only small loading/error/not-found/private/processing branches remain inline. Props wired faithfully; `setCommentSaved` deviation correctly preserves "open note resets Saved!" behavior. |
| 10.7 Vitest coverage for `processingIds` | Mock `useJobStatuses` + `Sidebar`; assert set = {job-a, job-c}, size 2 | VERIFIED — `CarePlanPage.test.tsx:205–226` asserts exactly that; full suite green (170/170). |

---

## PRD Compliance Summary

- **Goal 1 (delete `JobErrorData`):** Met (10.1).
- **Goal 2 (remove legacy ErrorDetail comment):** Met (10.4).
- **Goal 3 (retire `normalizeOutput.ts`):** Met unconditionally per resolved Q1 (10.5).
- **Goal 4 (wire `processingIds`):** Met, live `Set<string>` reaches `Sidebar` (10.3), test-locked (10.7).
- **Goal 5 (remove `flush()` TODO):** Met (10.2).
- **Goal 6 (optional component split):** Included per resolved Q2 and completed (10.6).
- **Non-goals respected:** No backend changes; `src/types/errors.ts` untouched; no UI-behavior changes (error indicator gating preserves prior visibility; result-view Sidebar still omits `processingIds`, matching pre-refactor behavior on the single-job page).
- **Type/test gate:** `tsc -b` clean, full Vitest suite green — satisfies PRD §2 / TASKS Definition of Done.
- **Open questions:** Q1 and Q2 were reconciled in PRD §9 (commit `17d3a74b`) before gated work proceeded; the first run's STOP was correctly resolved.

---

## Prioritized Issues

### Low / Informational (non-blocking)

1. **Verification-command gap in TASKS.md / PRD §7 (process, not code).**
   The prescribed `npx tsc --noEmit` (run bare from `frontend/`) type-checks **zero**
   files because root `frontend/tsconfig.json` has `"files": []` and only project
   `references`. This masked the original TS2352 break until `npm run build` was run.
   The code itself is now correct; the recommendation is to update the verification
   template to `npx tsc -b` (or `-p tsconfig.app.json`). Already documented in the run
   summaries; no source change required for SP10 to be considered done.

2. **Pre-existing lint debt (out of scope, not a regression).**
   `npm run lint` reports 12 problems (8 errors, 4 warnings) — `react-hooks/globals`
   in `AuthContext.test.tsx` and a triple-slash-reference in `vite.config.ts`. Confirmed
   identical on the pre-SP10 baseline (`17d3a74b`); not introduced by SP10. Worth a
   separate cleanup ticket.

### Notes (no action needed)

- `CarePlanJobPage.tsx:55` direct cast required the double-cast through `unknown`
  because `JobDoc.output_data` is `Record<string, unknown> | null`. The PRD §4c /
  TASKS 10.5 snippet's single-step `as CarePlanInternal` would not compile; the
  fixup commit (`bfb3b55c`) is the correct resolution.
- `processingIds` is recomputed (new `Set`) on every `CarePlanPage` render. This is
  exactly the PRD §4d / TASKS 10.3 specified form and is benign (`Sidebar` is not
  memoized; no functional or perf concern at this scale).
