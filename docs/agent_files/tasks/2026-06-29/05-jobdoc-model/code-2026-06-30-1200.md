# Code Run — 2026-06-30 12:00 — SP05 JobDoc Pydantic Model
Branch: users/tejitpabari/llm-code-check   Base: main

## Tasks completed

| Sub-project | Task | Status | Commit | Files | Tests |
|---|---|---|---|---|---|
| 05-jobdoc-model | Task 05.1 — Add JobDoc model | done | 68ac5b1f | constants.py, models/job.py, models/__init__.py | import ok, 31 fields present |
| 05-jobdoc-model | Task 05.2 — Unit tests for JobDoc | done | 3470d84d | tests/models/test_job.py | 11 passed |
| 05-jobdoc-model | Task 05.3 — Migrate worker.py read path | done | b42e3e00 | routes/worker.py | no dict accesses remain below parse line |
| 05-jobdoc-model | Task 05.4 — Update worker test fixtures | done | d9425c58 | tests/routes/test_worker.py, tests/routes/test_worker_gcs_dataset.py | 18 passed |
| 05-jobdoc-model | Task 05.5 — Full suite + fixes | done | c87b2eb8 | utils/gcs_datasets.py, constants.py, models/care_plan/__init__.py, routes/care_plan.py, tests/utils/test_llm.py, tests/utils/test_app_observability.py | 411 passed, 24 subtests passed |

## Deviations from the PRD/TASKS

- **Task 05.1 — 31 fields vs 29**: The model has 31 fields (`stage` and `started_at` are both declared); the PRD field table listed 29 but both were in the model definition. No functional deviation.
- **Task 05.2 — `test_output_data_typed_submodel`**: Uses a local `_make_care_plan_internal()` helper rather than importing from `test_envelope.py`, to avoid test-module import fragility.
- **Task 05.4 — Extra fixes beyond fixture updates**: Two inline dicts in `test_worker.py` (idempotency tests) were replaced with `_make_job_doc(status=...)` calls; a pre-existing `ImportError` for a never-exported constant was fixed.
- **Task 05.5 — 6 files fixed beyond SP05 scope**: Test isolation issues in `test_llm.py` and `test_app_observability.py` (pre-existing; surfaced when running full suite); `gcs_datasets.py` monkeypatching issue; `CARE_PLAN_VERSION` constant missing from `models.care_plan` exports. All were pre-existing bugs unmasked by the full run.

## Follow-ups / new issues discovered

- `routes/care_plan.py` imported `CARE_PLAN_VERSION` from `models.care_plan` which didn't export it — fixed in Task 05.5 by adding the constant to `constants.py` and the `__init__.py` export. A TODO in constants.py marks that SP03+ should remove the compat shims once all consumers migrate.
- `input_source_kind` is typed as `AthenaSourceKind` (a `StrEnum`). The enum name is slightly misleading now that it covers all source kinds (not just Athena ones). Renaming to `SourceKind` or `InputSourceKind` is a candidate cleanup for a later SP.
- SP06 can now do a mechanical swap of inline-dict construction in `batch_jobs.py` and `care_plan_jobs.py` to the three factories; the factories are complete and tested.

## Still requires you (from PRD §8 + this run)

- None. SP05 is a pure code change with no infrastructure, env var, or Firestore schema changes required. Normal code review + deploy cycle applies.
