# Code Run — 2026-06-22 06:00 — SP3: Ad Hoc UI Polish (all tasks)
Branch: new-features   Base: a4689f1

## Tasks completed

| Sub-project | Task | Status | Commit | Files | Tests |
|---|---|---|---|---|---|
| 03-ad-hoc-ui-polish | Task 1 — Compact Sidebar CSS + remove fixed width | done | cea5812 | Sidebar.css | tsc clean |
| 03-ad-hoc-ui-polish | Task 2 — Sidebar resize, collapse, processingIds prop | done | 3c25a56 | Sidebar.tsx | tsc clean |
| 03-ad-hoc-ui-polish | Task 3 — Sort sidebar items latest-first | done | 940d1db | groupSavedOutputs.ts, groupSavedOutputs.test.ts | 8 passed |
| 03-ad-hoc-ui-polish | Task 4 — NavBar restructure (three-column grid) | done | 7729390 | NavBar.tsx, App.css, CarePlanPage.tsx | tsc clean |
| 03-ad-hoc-ui-polish | Task 5 — CarePlanPage content wrapper CSS var + processingIds | done | 850473e | CarePlanPage.tsx | tsc clean |
| 03-ad-hoc-ui-polish | Task 6 — Remove deleted-note span + add timestamp | done | 7729390 | CarePlanPage.tsx, App.css | tsc clean |
| 03-ad-hoc-ui-polish | Task 7 — Upload page card gaps + NavBar-to-content spacing | done | 7729390 | App.css | tsc clean |
| 03-ad-hoc-ui-polish | Task 8 — Rename VersionsPage→ModelsPage + App.tsx routes | done | 7fc052b | ModelsPage.tsx (new), GradingVersionDetailPage.tsx (stub→replaced), App.tsx, VersionsPage.tsx (deleted) | tsc clean |
| 03-ad-hoc-ui-polish | Task 9 — Add GRADING_VERSIONS and GRADING_VERSION_DETAILS to config.ts | done | 50b8e32 | config.ts | tsc clean |
| 03-ad-hoc-ui-polish | Task 10 — Create GradingVersionDetailPage | done | bda8a4d | GradingVersionDetailPage.tsx | tsc clean |
| 03-ad-hoc-ui-polish | Task 11 — Unit tests for Sidebar collapse + processingIds | done | 41c781a | Sidebar.test.tsx (new) | 2 passed |

**Final test run:** 10 tests passed (groupSavedOutputs: 8, Sidebar: 2). TypeScript clean.

## Deviations from the PRD/TASKS

- **Tasks 4+6+7 committed together** — all three touch App.css; batched into a single commit (7729390) to avoid conflicts. Functionally identical to three separate commits.
- **Task 8 stub**: GradingVersionDetailPage was stubbed in the Task 8 commit (to satisfy the App.tsx import) and replaced with the real implementation in Task 10. The stub commit is 7fc052b; the replacement is bda8a4d.
- **Task 2 side effect**: The Task 2 agent also removed the stale `onNew={handleReset}` from `<Sidebar>` in CarePlanPage.tsx during TS compile (the prop had already been removed from SidebarProps). This was necessary for TS to pass and is correct per the PRD.
- **`isProcessing` logic in Sidebar**: When `processingIds` prop is provided (even empty set), it takes full precedence. When `processingIds` is `undefined`, falls back to the existing `output.status` field check (`isInProgress`). This is more backward-compatible than the TASKS.md sketch, which only checked `processingIds`.
- **Sidebar.test.tsx**: Added `vi.stubGlobal('localStorage', ...)` and `beforeEach(() => vi.clearAllMocks())` since Sidebar reads localStorage on init. These additions don't change what is tested.

## Follow-ups / new issues discovered

- The `handleDelete` in Sidebar no longer navigates away after deleting the active item (onNew was removed). The active item simply deselects. If a "navigate to root after delete" behavior is desired, a `useNavigate` call could be added to Sidebar — route to `dev-design` if needed.
- The `@keyframes spin` animation in Sidebar.css is now superseded by `@keyframes sidebar-spin` (Task 1 added the new one; the old `spin` keyframe remains for the inline-style spinner that was already there before SP3, now replaced). The old `@keyframes spin` at the bottom of Sidebar.css is now dead code — safe to remove in a cleanup pass.

## Still requires you (from PRD §8 + this run)

1. **Confirm `/docs/<slug>` URL scheme with SP4 (PRD §8.1).** SP3 hardcodes links like `/docs/smog`, `/docs/flesch-kincaid`, etc. in `GRADING_VERSION_DETAILS`. If SP4 uses a different path structure, update `docsSlug` values in `config.ts`.

2. **Confirm SP1's `useJobStatuses` hook interface (PRD §8.2) before wiring Task 5.** `processingIds` is currently `undefined` in CarePlanPage. A `// TODO: wire SP1` comment marks the location. When SP1 lands: import `useJobStatuses` from `../../api/useJobStatuses`, compute the Set from the statuses Map, and replace the `undefined` stub.

3. **Verify `GRADING_VERSION_DETAILS` breakdown sub-score key names (PRD §8.3).** The `breakdownKeys` arrays in `config.ts` were inferred from the PRD. Grep `backend/utils/scoring_methods.py` to verify each method's actual dict keys before shipping the GradingVersionDetailPage.
