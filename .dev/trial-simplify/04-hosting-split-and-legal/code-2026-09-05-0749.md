# Code Run — 2026-09-05 07:49 — SP4 Hosting Split, CI & Legal Pages
Branch: users/tejitpabari/trial-app   Base: main

## Tasks completed

| Sub-project | Task | Status | Commit | Files | Tests |
|---|---|---|---|---|---|
| SP4 | Task 1 — Verify one-time Hosting site/target setup (§4.2) | Done, verify-only, no commit | — | `.firebaserc` (read-only) | `cat .firebaserc` confirmed `trial`→`juno-medical-clarity`, `app`→`juno-app-99`, matching PRD §4.1 exactly |
| SP4 | Task 2 — Root `firebase.json` multi-target hosting block (§4.1) | Done (prior run) | `03bf3749` | `firebase.json` | n/a |
| SP4 | Task 3 — Delete `frontend/firebase.json` + `frontend/.firebaserc` (§4.1) | Done (prior run) | `8ccfaa3e` | `frontend/firebase.json`, `frontend/.firebaserc` | n/a |
| SP4 | Task 4 — Backend CORS widened for `juno-app-99.web.app` (§4.5) | Done (prior run) | `72d89e24` | `backend/app.py` | n/a |
| SP4 | Task 5 — `deploy.yml`: `--max-instances` for `juno-api`/`juno-worker` (§4.6) | Done | `0e7aafe0` | `.github/workflows/deploy.yml` | `python3 -c "import yaml..."` exit 0; grep checks below |
| SP4 | Task 6 — `deploy.yml`: multi-target frontend build+deploy (§4.3) | Done | `262f7720` | `.github/workflows/deploy.yml` | `python3 -c "import yaml..."` exit 0; grep checks below. Cannot be exercised end-to-end until SP3's `frontend-trial/package-lock.json`/build exist (expected caveat, already true — SP3 has landed on this branch, so the job is now buildable in principle, but was not run live since this is a workflow-file-only task) |
| SP4 | Task 7 — `deploy.yml`: deploy-on-merge trigger gated on CI (§4.8) | Done | `fbe0bc95` | `.github/workflows/deploy.yml` | `python3 -c "import yaml..."` exit 0; grep checks below |
| SP4 | Task 8 — Fix `rollback-production.yml` for multi-target + `min-instances=0` (§4.4) | Done | `d89c0e04` | `.github/workflows/rollback-production.yml` | `python3 -c "import yaml..."` exit 0; grep checks below |
| SP4 | Task 9 — `ci.yml`: `push` trigger + `frontend-trial` job (§4.7) | Done (reconciled, see below) | `752faee2` | `.github/workflows/ci.yml` | `python3 -c "import yaml..."` exit 0; grep checks below |

### Verification commands and trimmed output

**Task 5:**
```
$ python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"
(exit 0)
$ grep -n "API_MAX_INSTANCES\|WORKER_MAX_INSTANCES" .github/workflows/deploy.yml
24:      API_MAX_INSTANCES: "10"
25:      WORKER_MAX_INSTANCES: "5"
72:            --max-instances "$API_MAX_INSTANCES" \
85:            --max-instances "$WORKER_MAX_INSTANCES" \
```
`--memory`, `--timeout`, `--set-env-vars`, `--set-secrets` on both `gcloud run services
update` calls confirmed unchanged via `git diff` (only the one new flag line added to
each call, plus the two new `env:` entries).

**Task 6:**
```
$ python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"
(exit 0)
$ grep -n "working-directory: ./frontend" .github/workflows/deploy.yml
(no output — removed)
$ grep -n "deploy_target" .github/workflows/deploy.yml
146:          deploy_target() {
163/172:      deploy_target app   (Task 6 baseline; wrapped in Task 7's if-guard afterward)
164/175:      deploy_target trial
```
`cache-dependency-path` lists both `frontend/package-lock.json` and
`frontend-trial/package-lock.json`. "Build app (frontend/)" step's env block is
byte-for-byte identical to the pre-Task-6 job's `Build` step env block (diff confirmed
no drift — same 7 keys, same values). Exactly one "Deploy to Firebase Hosting" step
exists, calling `deploy_target app` before `deploy_target trial`.

**Task 7:**
```
$ python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"
(exit 0)
$ grep -n "workflow_run" .github/workflows/deploy.yml
4:  workflow_run:
23:      github.event.workflow_run.conclusion == 'success'
35/103/188:  ref: ${{ github.event.workflow_run.head_sha || github.sha }}   (x3, one per checkout)
```
`deploy-backend` carries the `if:` gate; `deploy-frontend` and `tag` carry no new `if:`.
All three `actions/checkout@v4` steps carry the `ref:` expression; `tag`'s checkout still
also has `fetch-depth: 0`. Exactly two `deploy-frontend` steps ("Install app
dependencies", "Build app (frontend/)") carry `if: github.event_name ==
'workflow_dispatch'`; the trial-target steps carry no such `if:`. The "Deploy to
Firebase Hosting" script wraps `deploy_target app` in the workflow_dispatch guard;
`deploy_target trial` is unconditional.

**Task 8:**
```
$ python3 -c "import yaml; yaml.safe_load(open('.github/workflows/rollback-production.yml'))"
(exit 0)
$ grep -n "target: app" .github/workflows/rollback-production.yml
152:          target: app
$ grep -n "entryPoint" .github/workflows/rollback-production.yml
151:          entryPoint: .
$ grep -n "min-instances" .github/workflows/rollback-production.yml
79:            --min-instances=0    (juno-worker, unchanged)
110:            --min-instances=0   (juno-api, fixed from =1)
$ grep -n "max-instances=3" .github/workflows/rollback-production.yml
80:            --max-instances=3    (juno-worker, byte-for-byte unchanged)
```
No `entryPoint: ./frontend` or `--min-instances=1` remain anywhere in the file.

**Task 9:**
```
$ python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"
(exit 0)
$ grep -n "push:" .github/workflows/ci.yml
6:  push:
$ grep -n "^  [a-z-]*:$" .github/workflows/ci.yml
14:  backend:
46:  frontend:
65:  frontend-trial:
```
`pull_request:` trigger still present, unchanged. Exactly three jobs. `backend` and
`frontend` jobs byte-for-byte unchanged (only the top-level `on:` block was touched).

## Reconciliation note (Task 9)

SP3's Task 19 (commit `2c522479`, already on this branch before this run started) had
already added a `frontend-trial` job to `ci.yml`, mirroring the `frontend` job with the
same four steps (`checkout`, `setup-node` with `cache-dependency-path:
frontend-trial/package-lock.json`, `Install dependencies` running `npm ci` in
`frontend-trial`, and `Run frontend-trial tests` running `npm run test` in
`frontend-trial`). This was read and diffed character-for-character against PRD §4.7's
"New" job spec and TASKS.md's Task 9 spec before touching anything — it matches exactly,
including step names. No second `frontend-trial` job was added. Task 9's only real gap
was the missing `push: branches: [main]` trigger, which is what this run's commit
`752faee2` adds. This is also functionally required for Task 7 to work at all: without
`ci.yml` running on `push` to `main`, no `workflow_run` event for the `"CI"` workflow
would ever fire on `main`, and `deploy.yml`'s new trigger would never activate
automatically.

## Deviations from the PRD/TASKS

None. All five tasks (5–9) were implemented exactly as specified in TASKS.md/PRD.md,
verified line-for-line against the PRD's literal "New" YAML blocks via `git diff`. No
scope creep: `firestore.rules`, `cloudbuild*.yaml`, `frontend/` and `frontend-trial/`
source, the SP2 trial-specific `deploy-backend` additions (`CLOUD_TASKS_QUEUE_TRIAL`,
"Ensure trial Cloud Tasks queue exists", `TRIAL_RATE_LIMIT_SALT`), and PRD §9 Q2's
deferred manual-approval gate were all left untouched, as instructed.

`actionlint` was not installed in this environment and could not be fetched (network
reachable but the release download script wasn't retrievable in the time available), so
verification relied on `python3 -c "import yaml,sys; yaml.safe_load(...)"` (parses valid
YAML but does not validate GitHub Actions schema/expression semantics) plus manual
line-by-line diff review against the PRD's literal blocks. This is a process note, not a
deviation in content.

## Follow-ups / new issues discovered

1. **`actionlint` is not available on this machine.** Recommend installing it (or
   running it via a GitHub Action step, e.g. `rhysd/actionlint` Docker image, in CI
   itself) so future workflow-file edits get real schema/expression validation instead
   of just YAML-parse validation. Not blocking — the five edited files were reviewed by
   hand against the PRD's exact literal blocks and match byte-for-byte.
2. **Tasks 6/7/9's `frontend-trial/` steps are still unexercised in a real CI/CD run.**
   SP3 has landed the app itself on this branch (with a `package-lock.json` and `dist`
   build per the task brief), so the workflow *should* now be exercisable — but this run
   only edited and statically validated the workflow YAML; it did not trigger an actual
   GitHub Actions run (that requires a push/PR against GitHub, outside this run's scope).
   Recommend watching the first real CI run on this branch/PR to confirm `frontend-trial`
   actually installs and tests cleanly, and the first real merge-triggered `deploy.yml`
   run to confirm `hosting:trial` deploys cleanly.
3. No other new issues found; nothing outside Tasks 5–9's stated scope needed touching.

## Still requires you (from PRD §8 + this run)

1. **Legal-copy review and approval (PRD §8 item 1, D8 — locked, still pending).** The
   Privacy Policy (PRD §6.2) and Terms & Conditions (PRD §6.3) are first drafts written
   by Claude, not reviewed by a lawyer. Before launch: read both in full, confirm the
   stated rate limit ("5 per hour," Terms §7) matches whatever SP2 actually ships, and
   confirm you're comfortable with the liability/warranty language for a health-adjacent
   public tool. This is a reading/judgment task only you can sign off on. The contact
   address (`tejitpabari99@gmail.com`) and deletion-timing wording are already corrected
   in the drafted text — this is about tone/liability comfort, not a pending text fix.
   No SP4 task transcribes this copy into `frontend-trial/` — that's SP3's territory;
   SP4's job ends at drafting the copy and the routing/footer contract.

2. **Cutover ordering is a hard constraint, not a suggestion (PRD §4.5).** The relocated
   full app only works at `juno-app-99.web.app` once Task 4's CORS change is live in
   production — it already is (Task 4 committed `72d89e24` on this branch), but you must
   still sequence the actual production rollout: deploy backend-only first if
   hand-triggering out of order, don't assume the ordinary
   `deploy-backend → deploy-frontend` sequence inside one workflow run is "early enough."
   Now that Task 7 has landed, merging to `main` will automatically CI-gate-deploy the
   backend + trial frontend — the `app` target remains `workflow_dispatch`-only, per
   design. Sequence your merge/dispatches per PRD §4.5 steps 1–8 (create sites → confirm
   `.firebaserc` → deploy CORS alone → merge → watch `app` deploy manually → verify →
   watch `trial` deploy automatically → verify with a non-PHI sample document). This is
   an operational act on live infrastructure that requires you (or whoever holds deploy
   credentials) to execute and watch it happen.

3. **Q2 — manual-approval gate between `app`/`trial` deploy steps — [DEFERRED], not
   implemented**, per PRD §9 Q2 and the initiative README. Flagging only so it isn't
   silently forgotten if the single-operator assumption ever changes.

4. **Rollback drill (PRD §7, recommended, not blocking).** After the cutover, at a
   low-stakes moment, trigger `rollback-production.yml` against a known `prod-*` tag to
   confirm Task 8's `target: app` / `entryPoint: .` fix actually works end-to-end.

5. **No new GitHub secret, IAM grant, or GCP resource is needed for Tasks 1–9.**
   `API_MAX_INSTANCES`/`WORKER_MAX_INSTANCES` are plain workflow `env:` literals, not
   secrets; `VITE_GA_MEASUREMENT_ID` is a literal GA4 ID, not sensitive; the Hosting
   sites/targets and the `FIREBASE_SERVICE_ACCOUNT` secret's IAM scope are both already
   confirmed sufficient.

6. **Watch the first real automatic `deploy.yml` run after this lands on `main`.** Since
   Task 7 makes `main` pushes CI-gated-automatic, the very next successful CI run on
   `main` will trigger a real production deploy of the backend + trial frontend (never
   `app`). Confirm this behaves as expected the first time it fires for real.
