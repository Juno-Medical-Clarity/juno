# Review — 2026-09-05 05:54 — SP1 (Image Input) + SP2 (Trial Backend), merged
Diff: 17c59ffe...ea3a03e5   Personas: Bug Hunter, Security, Architect

## Verdict: NEEDS_CHANGES  (resolved — see "Fix applied")

**Must-fix (1 finding, Architect):** `backend/utils/image_ocr.py:56-62` — `extract_text_from_image` raises `JunoError(ErrorCode.EMPTY_DOCUMENT)` on the `NO_TEXT_FOUND` sentinel, but it is called synchronously at request time from `services/care_plan_input.py::resolve_uploaded_files` (~:115) via `routes/care_plan_jobs.py::_resolve_input_for_job` (~:97-153) and `routes/trial.py::_resolve_trial_input` (~:68-114). Both routes caught only `(ValueError, FileNotFoundError)`, so the `JunoError` fell through to each route's generic `except Exception:` handler, which hardcodes `INTERNAL_ERROR`/500. Why it matters: uploading a single blurry or illegible photo — the routine case SP1 exists to serve — returned a bare 500 instead of `EMPTY_DOCUMENT`/422 with its user-facing hint, in BOTH the main app and the trial. SP1 introduced the first extractor that raises a content-dependent `JunoError` reachable from this synchronous call site (all others return `""` and let the worker's async stage-1 empty-text check in `worker.py:142-144` handle it), and no test exercised an image end-to-end through either route.

**Bug Hunter: zero findings.** Explicitly verified as correct: the `@firestore.transactional` read-then-conditional-write counter in `utils/rate_limit.py` (including its automatic retry-on-`Aborted` for the concurrent-5th-slot race, and its deliberate fail-open per PRD §4.2), hour-aligned window boundary math, rate-limit-before-auth decorator ordering in `routes/trial.py`, Cloud Tasks config validated before `create_job_doc` (so the trial route avoids the main app's orphan-doc ordering), hard-pinned `version`/`grading_enabled`/`doc_id`, `expires_at` computation, `worker.py`'s `job is not None` guard making trial GCS cleanup fire on both success and failure paths, `delete_gcs_object` URI parsing and `NotFound` swallowing, `to_firestore()`'s explicit `expires_at: null` on non-trial docs being TTL-safe, per-file try/except around image decode in `merge_pdfs`, and the SP1/SP2 auto-merge in `care_plan_input.py` composing cleanly.

**Security: zero findings.** Explicitly verified: `get_client_ip()`'s last-`X-Forwarded-For` choice is correct and non-spoofable for the current Cloud Run topology (no external LB/CDN in front — PRD §9 Q13 already documents the future-topology caveat); IP-keyed limiting is not resettable by minting fresh anonymous identities; `DELETE /trial/jobs/<id>` requires both `uid` match AND `is_trial` truthy, so it can reach neither another user's job nor a main-app doc; no escalation via client-supplied fields; `TRIAL_RATE_LIMIT_SALT` never logged and only present as a non-reversible truncated HMAC-SHA256 in doc IDs (`--set-env-vars` rather than Secret Manager is a pre-approved tradeoff, PRD §9 Q8); Pillow's `MAX_IMAGE_PIXELS` decompression-bomb guard is active and combines with the pre-existing 10MB/file and 25MB/aggregate caps; GCS paths use a server-generated `uuid4()` and the Firebase-verified `uid`, so no user filename reaches a path; the OCR prompt is transcription-only and its output enters the same trust boundary as every other extracted document; new log lines carry only `job_id`/opaque `gcs_uri`/static text.

**Architect notes (non-blocking, all clean):** `routes/trial.py` genuinely reuses `resolve_uploaded_files`, `upload_combined_pdf`, `JobDoc.for_single`, `create_job_doc`, `enqueue_job_safe`, and `require_env` rather than forking them; no cross-SP boundary leakage (no `--max-instances`, no TTL/lifecycle config, no `frontend-trial/`, and the pre-existing orphan-doc bug in `care_plan_jobs.py` left untouched as scoped); the `constants.py`/`care_plan_input.py` auto-merge is coherent, with `is_trial` defaulting false so existing call sites are unaffected and the trial route picking up image support for free through the shared dispatch; `POST`/`DELETE /trial/jobs` match PRD §5's shapes, status codes, and error-code table row by row.

## Fix applied

Commit `cba4b5ed` — caught `JunoError` alongside `(ValueError, FileNotFoundError)` in both routes' upload-resolution try blocks, translating via `exc.error_code` and `exc.info.http_status`. Chosen over making `extract_text_from_image` return `""` because it also fixes the identical latent fall-through for `UNSUPPORTED_FILE_TYPE` and `RESOURCE_NOT_FOUND`, in two lines per route, without threading a new signal up from the extractor. Two route-level regression tests added (`test_create_care_plan_job_blurry_image_returns_422_empty_document`, `test_post_blurry_image_returns_422_empty_document`), verified failing (`assert 500 == 422`) before the fix and passing after. Post-fix: 41 passed + 93 passed across the affected and wider merged test sets, no regressions.

## Next step

All must-fix findings resolved; SP1 and SP2 are ready to land. Proceed to SP3 (trial frontend), which depends on SP2's API contract. Note the two pre-existing, out-of-scope issues carried forward: the red `test_route_rewire.py` stale-blueprint-list test, and the dead `gemini-1.5-pro` `MODEL_DEFAULT`.
