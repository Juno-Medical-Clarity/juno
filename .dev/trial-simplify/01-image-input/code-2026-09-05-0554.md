# Code Run — 2026-09-05 05:54 — SP1 Image Input Support
Branch: users/tejitpabari/trial-app   Base: 17c59ffe

## Tasks completed

| Sub-project | Task | Status | Commit | Files | Tests |
|---|---|---|---|---|---|
| SP1 | 1. Add image deps to requirements.txt | done | b2754267 | backend/requirements.txt | pip install --dry-run clean |
| SP1 | 2. Verify pillow-heif HEIC decode on deploy image | blocked (no Docker in sandbox); substitute evidence gathered | — | — | wheel-tag + local HEIC round-trip passed |
| SP1 | 3. Image extensions + OCR prompt constant | done | 895f436f | backend/utils/constants.py | test_constants.py 10/10 |
| SP1 | 4. Extract `_generate_content` + add `generate_text_from_image` | done | b221f93b | backend/utils/llm.py | test_llm.py 11/11 |
| SP1 | 5. Update test_llm.py | done | 2631b634 | backend/tests/utils/test_llm.py | test_llm.py 16/16 |
| SP1 | 6+7+8. `image_ocr.py` module + optional downscale (Task 7 implemented, not dropped) + tests | done | c935dd4c | backend/utils/image_ocr.py, backend/tests/utils/test_image_ocr.py | test_image_ocr.py 7/7 |
| SP1 | 9. `pdf.py` HEIC registration, `_image_to_pdf`, merge_pdfs wiring | done | df9edf6c | backend/utils/pdf.py | test_pdf.py 4/4 |
| SP1 | 10. Extend test_pdf.py | done | 2eabb556 | backend/tests/utils/test_pdf.py | test_pdf.py 8/8 (HEIC test ran, not skipped) |
| SP1 | 11. `care_plan_input.py` dispatch wiring | done | 8d4b9646 | backend/services/care_plan_input.py | test_care_plan_input.py 3/3 |
| SP1 | 12. Extend test_care_plan_input.py | done | 77361506 | backend/tests/services/test_care_plan_input.py | 10/10 |
| SP1 | 13. Error-string update in care_plan_jobs.py | done | 84bfaaa6 | backend/routes/care_plan_jobs.py | n/a (cosmetic) |
| SP1 | 14. Extend test_care_plan_jobs.py | done | a87a1d04 | backend/tests/routes/test_care_plan_jobs.py | 8/8 |
| SP1 | 15. Live Vertex smoke test | done — no fallback code needed | — | — | see deviations |
| SP1 | 16. frontend constants.ts | done | 0032b4f9 | frontend/src/constants.ts | grep check OK |
| SP1 | 17. CarePlanPage.tsx accept images | done | 2da37b23 | frontend/src/pages/care-plan/CarePlanPage.tsx | npm run build succeeds, no new TS errors |
| SP1 | 18. Run SP1-relevant tests | done, no regressions | — | — | 59 passed |

Implemented in an isolated worktree, then merged via merge commit `1e343574` ("Merge SP1 (image input) into trial-app"). All commit SHAs above were cross-checked against `git log --oneline 17c59ffe..HEAD` and match exactly — no discrepancies found.

## Deviations from the PRD/TASKS

- **Task 2:** Docker unavailable in the sandbox, so the literal `docker build` check could not run. Substituted: confirmed `pillow-heif` 1.6.0 ships a manylinux_2_27/2_28 x86_64 cp311 wheel (matching `python:3.11-slim`, Debian bookworm glibc 2.36) and ran a full local HEIC encode→decode round-trip, which passed. No `Dockerfile` change made. The literal build check remains an owner step.
- **Task 15:** live smoke test ran successfully — the production model `gemini-3.5-flash` accepted real HEIC bytes natively and transcribed the embedded text exactly; no JPEG-fallback code was needed. Separately, `gemini-1.5-pro` (the `MODEL_DEFAULT` constant) returned `NotFound` at both `global` and `us-central1`; a plain text-only call confirmed this is a pre-existing model-availability issue unrelated to images.
- **Task 7 (optional):** implemented rather than dropped — ~15 lines with a safe try/except fallback, meeting the PRD's own cost/benefit criteria.

## Follow-ups / new issues discovered

- `gemini-1.5-pro` (`Constants.Llm.MODEL_DEFAULT`) appears unavailable in the `juno-medical-clarity` project. Production overrides it via `VERTEX_AI_MODEL=gemini-3.5-flash` in `deploy.yml`, so deployed behavior is fine, but the default is dead for local dev or any consumer constructing `LLMClient()` with no env override. → worth a follow-up ticket to update `MODEL_DEFAULT`; out of SP1 scope.
- No `backend/.venv` exists in fresh git worktrees (it lives only in the main checkout); pip installs there needed `--break-system-packages` under Debian's PEP 668 guard. → tooling note.

## Still requires you (from PRD §8 + this run)

- Run the literal `cd backend && docker build ...` + HEIC decode check once in a Docker-capable environment (PRD §8 item 1); falls back to adding `apt-get install libheif1` to `backend/Dockerfile` only if it fails.
- Cost awareness (PRD §8 item 2): each image upload now costs one extra Vertex vision call. Informational.
- Decide a replacement for the dead `MODEL_DEFAULT` (not blocking).
